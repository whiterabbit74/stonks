# Отчет по перспективам унификации кодовой базы

На основе анализа ключевых компонентов (`SingleTickerPage`, `MultiTickerPage`, `MultiTickerOptionsPage`, `BacktestResultsView`), выявлены следующие области для оптимизации и устранения дублирования кода.

## 1. Выделение логики данных для одиночного тикера
**Текущая ситуация:**
Компонент `SingleTickerPage` перегружен логикой (более 300 строк), отвечающей за:
- Проверку актуальности данных (`isStale`, проверка выходных/праздников).
- Определение статуса рынка (открыт/закрыт).
- Взаимодействие с Telegram-мониторингом (`watching`).
- Периодический опрос котировок (`fetchQuote`).

В то время как мульти-тикерные страницы уже используют хук `useMultiTickerData`.

**Предложение:**
Создать хук `useSingleTickerData`, который инкапсулирует всю логику загрузки, валидации и обновления данных для одного актива.

*   **Сложность:** Средняя (Medium). Требует аккуратного переноса `useEffect` и состояний без нарушения циклов ререндеринга.
*   **Выгода:** Значительное упрощение `SingleTickerPage`, возможность переиспользования логики проверки "свежести" данных.

## 2. Унификация карточек конфигурации (Parameters UI)
**Текущая ситуация:**
В `MultiTickerPage` и `MultiTickerOptionsPage` полностью продублирован UI-код блока "Параметры" (карточка с градиентным фоном, иконкой, сеткой инпутов). Код повторяется почти дословно, отличаясь только набором полей.

**Предложение:**
Создать компонент `StrategyConfigurationCard` (или `ParametersPanel`), который принимает `children` (инпуты) или массив конфигурации полей.

*   **Сложность:** Низкая (Low).
*   **Выгода:** Единый стиль, проще менять дизайн панели параметров в будущем.

## 3. Централизация математики метрик (Unified Math Layer)
**Текущая ситуация:**
- `MultiTickerPage` вручную маппит результаты бэктеста.
- `MultiTickerOptionsPage` *инлайн* (прямо внутри функции запуска) рассчитывает CAGR, Profit Factor, Win Rate и Max Drawdown.
- `SingleTickerPage` получает готовые метрики из стора.

Это создает риск расхождения цифр между режимами (например, формула CAGR или Profit Factor может отличаться в разных файлах).

**Предложение:**
Расширить утилиту `lib/trade-utils.ts` или создать класс `BacktestStatistics`, который принимает сырые данные (Equity + Trades) и возвращает стандартизированный объект `BacktestMetrics`, используемый во всех компонентах отображения.

*   **Сложность:** Низкая (Low).
*   **Выгода:** Гарантия идентичности расчетов (CAGR, PF, DD) во всем приложении.

## 4. Выделение представления "Monthly Contribution"
**Текущая ситуация:**
В `MultiTickerPage` находится массивный блок JSX кода (около 200 строк), отвечающий за рендеринг таба "Пополнения" (сравнение сценариев, графики, карточки дельты). Это делает основной компонент трудночитаемым.

**Предложение:**
Вынести этот блок в отдельный компонент `MonthlyContributionAnalysis`.

*   **Сложность:** Низкая (Low).
*   **Выгода:** Улучшение читаемости кода, изоляция логики сравнения сценариев.

## 5. Унификация обработки состояний загрузки (Page Shell)
**Текущая ситуация:**
`SingleTickerPage` имеет сложную логику отображения лоадеров и экранов выбора тикера. Мульти-страницы имеют свои упрощенные версии "Loading...".

**Предложение:**
Создать компонент-обертку `BacktestPageShell`, который принимает пропсы `isLoading`, `error`, `isDataReady` и стандартизирует отображение спиннеров и сообщений об ошибках.

*   **Сложность:** Средняя (Medium).
*   **Выгода:** Единый UX при загрузке и ошибках.

## Сводная таблица приоритетов

| Задача | Приоритет | Сложность | Влияние на качество |
| :--- | :---: | :---: | :---: |
| **Logic Extraction (Hooks)** | 🔥 Высокий | Medium | Высокое |
| **Unified Math Layer** | 🔥 Высокий | Low | Критическое (точность данных) |
| **Monthly Contribution UI** | Средний | Low | Улучшение читаемости |
| **Configuration UI** | Средний | Low | UI Консистентность |
| **Page Shell** | Низкий | Medium | UX Консистентность |
