# Реестр аудитов и находок

Единый индекс по текущему checkout `main` на ревизии `ec99307` (2026-09-06).
Статусы не наследуются автоматически из старых документов: при конфликте
используется `NEEDS_RECHECK`.

| ISSUE-ID | Корень проблемы | Источники | Статус | Коммит | Проверка |
|---|---|---|---|---|---|
| AUD-001 | Неатомарное закрытие сделки/связанных строк может дать двойное закрытие или неверный P&L (прибыль/убыток) | `archive/AUDIT_FULL_2026-09-05.md` B1, H1 | FIXED_UNVERIFIED | `4aec299`, `6e93799` | `go test -race ./...` PASS; отдельного конкурентного CloseTradeByID-сценария нет |
| AUD-002 | Ошибки чтения/записи состояния превращаются в безопасно выглядящий успех или отсутствие данных | `archive/AUDIT_FULL_2026-09-05.md` (коммит `2f52268`, файл удалён при переносе `1296575`/`30c33d7`) B2, B3, B5; `docs/audits/AUDIT_2026-09-05.md` B1 | FIXED_UNVERIFIED | `e8f6ee2`, `1293315`, `bbef08e`, `c9e445c`, `8887d8f` | Три исходных источника закрыты и перепроверены чтением кода на `1293315`: **B2** — 14 циклов `rows.Next()` в `internal/store`, ровно 14 вызовов `rows.Err()`, обрезанной выборки под видом полной больше нет; **B3** — `PUT/PATCH /api/settings` идёт через `SetSettingsKeys` (`server.go:591,608`), `SaveSettings` из хендлеров не вызывается, предохранитель `trackerPersistFail` больше не снимается сохранением настроек; **B5** — `PatchTrade` (`db.go:1484-1527`) целиком в одной транзакции с `defer Rollback`. Ссылка на `AUDIT_2026-09-05.md` B1 относится к росту access-лога и покрыта AUD-009b, а не этим корнем. Новые находки этого прохода — AUD-002a и AUD-002b, обе исправлены. **Отказной охват сделан на `8887d8f`** — проход по всему репозиторию (`, _ :=` по всем пакетам, отфильтровано до чтений состояния БД/брокера). Найдены и исправлены AUD-002c…AUD-002g. Остаются NEEDS_RECHECK внутри AUD-002 (разобраны чтением кода, тестов нет, каждое место fail-closed либо только отчётное): `ema.go:26,89` и `telegram.go:664 ListSplits` — без сплитов `EvaluatePriceIntegrity` блокирует **больше**, а не меньше; `telegram.go:657 GetOHLCLast` — пустые бары дают `ev.ok=false`, сигнала нет; `telegram.go:704,728` (`openMonitorTrade`, история сделок) — только текст сообщения, ордера идут через fail-closed `EvaluateWindow`; `actualize.go:64,116,120` — отказ даёт `no_tickers` и алерт «Актуализация цен не удалась»; `scheduler.go:498 T1ExecutionFinished` — обе ветки шлют алерт, отличается формулировка; `store/robinhood.go:16 GetRobinhoodOAuth` — пустая строка читается как «не подключено», торговля блокируется; эхо `ListSplits`/`GetDataset` в ответах ручек (`server.go:648,666,873,1050,1069,1097`) и `GetCalendar` в `handleGetCalendar`/`handlePrevDay` — отказ показывает пустой список или дефолтный календарь в UI, записей не делает |
| AUD-002a | Недоступный календарь неотличим от пустого, и торговый день идёт по непроверенному расписанию. `scheduler.RunTick` брал `raw, _ := db.GetCalendar()`: при отказе чтения `ParseCalendar` давал пустую карту, `IsTradingDay` уходил в вычисляемый fallback, `calendarCoverageThrough("")` возвращал пустую строку и не срабатывал уже существующий предохранитель — T-11/T-1 запускались на расписании, которого никто не подтверждал. Тот же корень в `live.sessionCloseMin`: отказ чтения и битый JSON молча давали дефолт 16:00, то есть на коротком дне закрытие уезжало на три часа, а вместе с ним `outsideExecutionWindow` и бюджет повторов `t1Deadline` | текущая проверка на `652dbbf`; `go/internal/scheduler/scheduler.go` RunTick, `go/internal/live/telegram_t11.go` sessionCloseMin | FIXED_UNVERIFIED | `e8f6ee2` | Тик переиспользует ветку coverage-expired: отказ чтения даёт `market-jobs skipped=calendar-read-failed`, алерт «Календарь биржи недоступен» раз в день и не запускает T-11/T-1. `sessionCloseMin` вернула ошибку; `outsideExecutionWindow` при неизвестном закрытии блокирует, `t1Deadline` не выдаёт бюджет повторов (первая попытка по-прежнему уходит — задокументированное поведение). Тесты `TestAUD002CalendarReadFailureSkipsMarketJobs` (доказано падающим на старом коде: до фикса тик выполнял `telegram-aggregation until=1`) и `TestAUD002UnknownCloseIsFailClosed`. `go test ./...`, `go test -race ./...`, `go vet`, `gofmt -l` чисто на `1293315`, TZ=Pacific/Auckland и TZ=America/Los_Angeles тоже. Не задеплоено |
| AUD-002b | Нечитаемый список наблюдения выглядел как пустой. `configuredSymbols` брал `rows, _ := e.DB.ListWatches()` и при отказе возвращал пустую вселенную — решение `empty_symbol_universe`, то есть обычный день «ни один тикер не подошёл»; рядом `EvaluateWindow` вторым чтением той же таблицы наполнял `watchBy` только при `err == nil`, поэтому отказ молча подменял пороги каждого тикера глобальными дефолтами | текущая проверка на `652dbbf`; `go/internal/live/config.go` configuredSymbols, `go/internal/live/autotrade.go` EvaluateWindow | FIXED_UNVERIFIED | `1293315` | Одно чтение кормит и вселенную, и пороги; отказ даёт решение `watchlist_unavailable` («список тикеров не читается — вход заблокирован») вместо выдуманной пустоты. Двойное чтение таблицы убрано. Тест `TestAUD002UnreadableWatchlistIsNotEmptyUniverse`. `TestJournalReadErrorBlocksEvaluate` уточнён: он ронял всю БД и теперь ронял бы сперва watchlist-гард, поэтому роняет только `broker_trades` и продолжает проверять именно журнальный путь. Полный прогон — как в AUD-002a. Не задеплоено |
| AUD-002c | Нечитаемый журнал считался «позиция закрыта». `awaitFlatAfterExit` брал `rows, _ := e.DB.ListTrades("broker_trades")`, и отказ чтения давал тот же ответ, что и закрытая позиция, — «flat». В `runT1Orders` это разрешает повторный вход поверх позиции, которая, возможно, ещё открыта | текущая проверка на `ec99307`; `go/internal/live/track.go` awaitFlatAfterExit | FIXED_UNVERIFIED | `bbef08e` | Чтение вынесено в `journalFlat`, ошибка логируется как `journal_read_failed` и никогда не читается как flat; при отказе `awaitFlatAfterExit` возвращает false, повторного входа нет. Тест `TestAUD002UnreadableJournalIsNotFlat` доказан падающим на предфиксном коде (старый код отвечал flat). Не задеплоено |
| AUD-002d | Отказ чтения журнала стирал признак открытой позиции у всех тикеров. `UpdatePositions` брал `watches, _ := ListWatches()` и `monitor, _ := ListTrades("trades")`, при отказе получал «нет открытых сделок» и патчил каждый watch `isOpenPosition=false`, `currentTradeId=nil`, возвращая при этом `success: true` | текущая проверка на `ec99307`; `go/internal/live/actualize.go` UpdatePositions | FIXED_UNVERIFIED | `bbef08e` | Оба чтения проверяются, отказ даёт `success:false`, `error:state_read_failed` и ни одного `PatchWatch`. Тест `TestAUD002UnreadableJournalDoesNotClearOpenWatches`; на предфиксном коде он показывал `changes:[{AAPL isOpenPosition:false}] success:true`. Не задеплоено |
| AUD-002e | Исполненный брокером выход не попадал в журнал молча. `getTrade`/`openTradeBySymbol` глотали ошибку и возвращали nil, поэтому в `recordFill` ветка `action == "exit"` при отказе чтения не закрывала ни одной строки, не писала в лог и не поднимала предохранитель: у брокера позиции нет, в журнале она открыта. Тот же корень в `reduceOpenQuantity` (частичный выход терялся) и в ветке `entry` (проверка «строка уже есть») | текущая проверка на `ec99307`; `go/internal/live/trade_record.go`, `go/internal/live/monitor.go` | FIXED_UNVERIFIED | `bbef08e` | Хелперы возвращают ошибку; ветки entry/exit и `reduceOpenQuantity` при отказе пишут `local_trade_record_failed`/`local_trade_close_failed` и поднимают `raiseTrackerPersistBlock`; `insertJournalRow` считает «строка уже есть» только при успешном чтении; `applyConsistencyAction` не вставляет проекцию по нечитаемому ответу. Тест `TestAUD002UnreadableJournalOnExitFillRaisesBlock` доказан падающим на предфиксном коде. Не задеплоено |
| AUD-002f | Запись поверх состояния, которое не удалось прочитать. `PATCH /api/trading-calendar/day` — read-modify-write всего блоба календаря: отказ чтения давал пустой `cal`, и один патченный день сохранялся поверх всех праздников и коротких дней. `POST /api/datasets/{id}/apply-splits` при нечитаемой таблице сплитов отвечал `success:true, alreadyApplied:true` («датасет уже пересчитан»). `PatchAutoConfigChecked` мержил правки в **дефолтный** `autoTrading` (`Settings()` отдаёт дефолты при отказе) и сохранял их, сбрасывая пороги и флаги брокеров. `Account()` показывал нечитаемый список позиций как «позиций нет» | текущая проверка на `ec99307`; `go/internal/httpapi/server.go` handlePatchCalendarDay, `go/internal/httpapi/splits_apply.go`, `go/internal/live/autotrade.go` PatchAutoConfigChecked/Account | FIXED_UNVERIFIED | `c9e445c` | Календарь и сплиты отвечают 500 вместо записи и вместо ложного «уже применено»; `PatchAutoConfigChecked` идёт через `SettingsErr` и возвращает ошибку; `Account()` кладёт `positionsError`. Тест `TestAUD002UnreadableSplitsDoNotReportAdjusted` доказан падающим (старый код: 200 `alreadyApplied`). `TestAUD002UnreadableCalendarDoesNotOverwriteIt` фиксирует гарантию, но **не различает** старый и новый код: доступная инъекция ломает и запись тоже, так что старый код падал позже, на `SaveCalendar`. `PatchAutoConfigChecked` — тоже упрочнение без различающего теста: `SetSettingsKeys` читает настройки сам и при том же отказе возвращает ошибку, поэтому сброс дефолтами достижим только при плавающем отказе чтения между двумя вызовами. Не задеплоено |
| AUD-002g | Нечитаемый датасет = «данных нет». Calc-ручки брали `ds, _ := s.DB.GetDataset(...)`, поэтому отказ хранилища возвращался как 200 с бэктестом на ноль сделок; `PUT /api/datasets/{id}` мержит патч в сохранённый датасет и читал отказ как «ещё ничего не сохранено» | текущая проверка на `ec99307`; `go/internal/httpapi/calc.go` barsOrDataset/barsWithSplits/tickersOrOne/barsForSymbol, `go/internal/httpapi/server.go` handlePutDataset | FIXED_UNVERIFIED | `8887d8f` | Ошибка чтения доходит до ручки, ответ 500 «Не удалось прочитать датасет/котировки». Тесты `TestAUD002UnreadableDatasetIsNotAnEmptyBacktest` (старый код: 200 с `totalTrades:0`) и `TestAUD002UnreadableDatasetIsNotOverwrittenByPatch` (старый код: 400 «empty bars» — до порчи данных не доходило, валидация payload отбивала запись; дефект был в потерянном мерже и вводящем в заблуждение ответе). Не задеплоено |
| AUD-003 | Брокер теряется или заменяется default-брокером в исполнении, трекере, ручном закрытии и сверке | `archive/AUTOTRADE_READONLY_AUDIT_2026-09-04.md` F02, F04, F05; `archive/AUDIT_FULL_2026-09-05.md` H1 | VERIFIED | `5e6f0f2`, `85e5c84`, `3931311`; тест `32b1ae6` | `go test -race -run TestAUD003BrokerContourEndToEnd ./internal/live/` PASS на `32b1ae6`: сквозной сценарий на двух брокерах сразу — трекер и запись журнала несут имя своего брокера, филл Webull не журналируется под Robinhood, рестарт не дублирует. Проверено на TZ=Pacific/Auckland и TZ=America/Los_Angeles. Живой broker API по-прежнему не проверен |
| AUD-004 | Заявка/частичное исполнение не имеет устойчивого идемпотентного (повторобезопасного) журнала | `archive/AUTOTRADE_READONLY_AUDIT_2026-09-04.md` F03, F07, F08 | VERIFIED | `6e93799`, `19ec3cc`, `3931311`; тест `32b1ae6` | `go test -race ./internal/live/` PASS на `32b1ae6`. Доказано: listing lag и неизвестный статус не финализируют трекер и не вызывают повторную отправку; частичное исполнение не закрывает заявку; рестарт через новый Engine на той же БД дорешает pending и не дублирует строки; повторный PollTrackers идемпотентен; одновременные ручное `ClosePosition` и авто-выход дают ровно один SELL и один exit-трекер (общий ключ `reservations`). Побочный отказ INSERT см. AUD-012 |
| AUD-005 | Планировщик пропускает узкое окно T-11/T-1 или отмечает задачу выполненной до результата | `archive/AUDIT_FULL_2026-09-05.md` C2; `docs/audits/AUDIT_2026-09-05.md` D1-D3 | VERIFIED | `47676f8`, `5519aa8`; тест `ce76c80` | `go test -race ./...` PASS на `ce76c80`, TZ=Pacific/Auckland и TZ=America/Los_Angeles тоже. Покрытие 7 сценариев: задержанный тик — `TestTickMissedT11AtT8Alerts`, `TestTickMissedT1AfterCloseAlerts`, `TestReportMissedT1ParallelSendsOnce`; медленный брокер — новый `TestAUD005SlowBrokerStopsAtDeadline` (первый пробел: `ErrExecutionDeadlineExceeded` вообще не был покрыт тестами); сбой Telegram — `TestT1ExecutionFinishedRetriesReportOnly` (порядок в `telegram.go`: ордера → `MarkT1ExecutionFinished` → отправка → `MarkT1ReportSent`, повтор шлёт только отчёт); сбой записи lease — новый `TestAUD005LeaseWriteFailureIsFailClosed` (второй пробел; fail-closed подтверждён); повторный запуск — `TestTickT1SecondTickDoesNotPlace`, `TestT1ParallelLeaseSingleExecute`, `BeginT1Attempt` — честный CAS по `RowsAffected`; короткий день — `TestShortSessionClose`, `TestImportedShortDaysOverrideComputed`; остановка во время T-1 — новый `TestAUD005StopDuringT1LeavesNoOrder`. Побочная находка при остановке: AUD-013 |
| AUD-005a | Заявка на слот пропущенного T-11/T-1 сжигалась при неудачной отправке. `ClaimAggregateT11`/`ClaimMissedT1` берутся до `Send` (это нужно против параллельных тиков), но claim оставался взятым и когда отправка провалилась: один сбой Telegram — и единственное предупреждение за день терялось насовсем. В том же файле `lastCalendarCoverageAlertDate` уже помечался только после успешной отправки | находка прохода T-11/T-1 2026-09-06 на `0f4f6b9`; `go/internal/scheduler/scheduler.go` `reportMissedTelegram`, `go/internal/store/live_persist.go` | VERIFIED | `ec99307` | Добавлены `DB.ReleaseAggregateT11`/`ReleaseMissedT1`, claim возвращается при ошибке `Send`. `TestMissedAlertRetriesAfterFailedSend` (первая отправка падает → алерт уходит на следующем тике → третий тик не дублирует) FAIL на предфиксном коде, PASS на `ec99307`; `TestReportMissedT1ParallelSendsOnce` по-прежнему PASS. `go test -race ./...` PASS в TZ=Pacific/Auckland и TZ=America/Los_Angeles |
| AUD-005b | T-1 исполнился, отчёт не ушёл — и день оставался молчаливым навсегда. `MarkT1ExecutionFinished` ставится до отправки отчёта намеренно (ордера уже у брокера), но этот же флаг подавлял послезакрытийный алерт в `reportMissedTelegram`, а повтор отчёта живёт только внутри минутного окна. Telegram, недоступный всё это окно, оставлял выставленные заявки вообще без сообщения | находка прохода T-11/T-1 2026-09-06 на `0f4f6b9`; `go/internal/scheduler/scheduler.go` `reportMissedTelegram`; пробел «тихий день» из `docs/audits/AUDIT_2026-09-06.md` §6 | VERIFIED | `1d76bd6` | Исполненный T-1 без отчёта теперь шлёт отдельный «T-1 без отчёта» вместо «Пропущен T-1» и вместо тишины. `TestTickT1ExecutedWithoutReportAlerts` FAIL на `0f4f6b9`, PASS на `ec99307`. **Пробел:** сам отчёт после окна не пересобирается, оператора отправляют в журнал сделок |
| AUD-006 | Дубли правил и расхождение Go/SPA: IBS, CAGR, комиссии, сплиты, календарь | исторические audit/roadmap отчёты проекта; полная сверка на `ce76c80` | FIXED_UNVERIFIED | `9fc73b4`, `2f8bbac`, `f8842e9`, `ca2a414` | Все четыре расхождения закрыты, тесты приложены к каждому коммиту. `go test ./...`, `go test -race ./...`, `go vet ./...`, `gofmt -l` — чисто на `ca2a414`; TZ=Pacific/Auckland и TZ=America/Los_Angeles тоже; linux/amd64 сборка как в deploy.sh проходит. Не задеплоено. На проде работает образ `stonks-server:368a00b`, то есть до этих правок. |
| AUD-006a | Комиссия применяется только в одном движке из пяти. `commission()` живёт в `backtest/single.go` и вызывается только оттуда; `RunClean`, `RunNoStopLoss`, `RunEmaZone`, `RunMultiOptions`, `RunBuyHold` её не применяют, хотя `httpapi/helpers.go` подставляет `RiskManagement.Commission` в стратегию, которую они получают. Пользователь задаёт комиссию — в четырёх движках она молча игнорируется | сверка на `ce76c80`; `go/internal/backtest/single.go` commission(); маршруты `go/internal/httpapi/calc.go` | FIXED_UNVERIFIED | `2f8bbac` | `RunClean` берёт комиссию с обеих ног и вычитает из капитала ровно как `RunSinglePosition`, включая отказ от входа, который не покрывается балансом с учётом сбора. Тест `TestAUD006CleanAppliesCommission`. Дефолтная комиссия — ноль, ни один golden не сдвинулся. |
| AUD-006b | Две живые реализации CAGR расходятся на вырожденных периодах. `metrics.New(...).All()` (используют `RunClean`, `margin.go`, `httpapi/calc.go`) берёт `tradingPeriodYears()` с полом в 1/365.25 суток; `metrics.BacktestMetrics()` (используют `RunSinglePosition`, `RunEmaZone`) при `len(equity)<2` или нулевом периоде возвращает 0 | сверка на `ce76c80`; `go/internal/metrics/metrics.go` cagr/tradingPeriodYears против BacktestMetrics | FIXED_UNVERIFIED | `9fc73b4` | `tradingPeriodYears` больше не поднимает вырожденный период до суток: одна точка equity и старт равный концу дают 0, как в `BacktestMetrics`. Тест `TestAUD006CagrPathsAgree` сверяет оба пути. Мёртвая третья копия `metrics.CAGR` удалена. |
| AUD-006c | Торговый календарь в SPA не имеет вычисляемого fallback, который есть в Go. `scheduler.IsTradingDay` дополняет карту календаря `tradingdate.IsNYSEHoliday`, а `IsShortDay` — `computedShortDay`/`ShortDayName`; `app.js isMarketOpenNow()` смотрит только карту. Засеянный календарь покрывает 2025-2027, поэтому расхождение латентно до конца 2027 — и проявляется сразу, если импортированный календарь уже | сверка на `ce76c80`; `go/internal/scheduler/scheduler.go` IsTradingDay/IsShortDay против `go/web/js/app.js` isMarketOpenNow | FIXED_UNVERIFIED | `f8842e9` | `scheduler.FillComputedDays` дописывает вычисленные праздники и короткие дни в payload, который отдаёт `GET /api/trading-calendar`; алгоритм в JS не дублируется. Сохранённые записи не перезаписываются. Тесты `TestAUD006ServedCalendarMatchesGoDecision` (2026-2030, посуточно) и `TestAUD006FillKeepsStoredEntries`. |
| AUD-006d | Формула IBS существует в трёх местах с разным поведением на краях: `indicators.IBS` (при `high==low` возвращает 0.5, невалидный бар — NaN), `live/telegram.go` (guard `high>low`, затем кламп в [0,1] — цена вне сессионного диапазона не искажает ранжирование), `app.js` (`(last-low)/(high-low)` без клампа). Оператор в таблице наблюдения видит IBS больше 1, тогда как движок действует по клампнутому значению | сверка на `ce76c80`; `go/internal/indicators/indicators.go:138`, `go/internal/live/telegram.go:680-694`, `go/web/js/app.js:2832` | FIXED_UNVERIFIED | `ca2a414` | `app.js` требует `high > low` и клампит в [0,1] — та же формула, что в `live/telegram.go`. `node --check` проходит, UI-контракт-тесты зелёные. |
| AUD-007 | API и UI страниц брокеров показывают чужие/неполные данные или неразличимые статусы | исторический audit отчёт страниц брокеров | FIXED_UNVERIFIED | `f635dfb`, предыдущие broker/API commits | `go test ./internal/httpapi ./internal/live` PASS; авторизованный Webull/Robinhood empty-state обход PASS, broker runtime и заполненные ответы без живых credentials не проверены |
| AUD-008 | Адаптивный UI перекрывает, обрезает или скрывает контент; часть исправлений ещё без визуальной перепроверки | `docs/audits/UI_VISUAL_AUDIT_2026-09-05.md`; `visual-audit*.md` | VERIFIED | `d1b8a4d`, `b0f4d83`, `251946c`, `652dbbf` | Авторизованный Playwright/CUA-прогон localhost `127.0.0.1:8080` 2026-09-06: `/split`, `/enhance`, `/webull`, `/robinhood`, `/settings` на 360/768/1024/1440; `scrollWidth === viewport`, мобильные карточки сплитов, scroll-cue, icon-only actions, Webull autotrade и tablet-footer проверены. Заполненные графики Stocks/EMA/Options проверены на 1440 и 360; графики не выходят за внутренний контейнер. Broker empty-state показывает русские статусы, технические данные свернуты. |
| AUD-009 | HTTP-журнал, резервные архивы, Go/образ и контейнер имеют эксплуатационные риски | `archive/SECURITY_AUDIT.md`; исторический security roadmap; проверка на `eafa3a7` | VERIFIED (частично) | `145de3d` | Проверено локально на `eafa3a7`, четыре пункта из шести чисты. **Архивы:** `backup-from-server.sh` — `umask 077`, `mkdir -m 700`, `chmod 600` на архиве; локальная симуляция даёт `drwx------` / `-rw-------` и на стороне сервера, и после скачивания; других скриптов, создающих архивы, нет. **Go в бинарнике:** сборка ровно как в `deploy.sh` даёт `go version -m` = `go1.25.14`, статический ELF x86-64, `CGO_ENABLED=0`, `-trimpath`, stripped, `vcs.revision` вшит; `go.mod` объявляет 1.25.13, тулчейн новее — норма. **govulncheck** (source-mode, `./...`): 0 достижимых уязвимостей. 3 в required modules — `golang.org/x/crypto@v0.55.0`: GO-2026-6355 и GO-2026-6354 (DoS в `ssh`, чинятся в v0.56.0) и GO-2026-5932 (`openpgp` не поддерживается, фикса нет). Ни один не вызывается: `go list -deps ./cmd/server` даёт из x/crypto только `bcrypt`/`blowfish`, а `strings` по бинарю — 0 вхождений `ssh` и `openpgp`. `go get -u golang.org/x/crypto` закроет две строки отчёта. **Ротация HTTP-журнала:** прогон `appendHTTPLog` при `httpLogMaxBytes=512` — ровно 4 файла (base + `.1`/`.2`/`.3`), все `-rw-r-----` (0640, не world-readable). Остальное — AUD-009a..c. Прод проверен 2026-09-06 по прямому разрешению: рабочий конфиг-каталог и его `.env` — 700/600, HTTP-журнал в контейнере `-rw-r-----`, лимиты контейнеров совпадают с compose (`server` Memory=2g PidsLimit=512 CapDrop=ALL non-root uid 10001 **NanoCpus=0**, `caddy` — всё пусто, что и есть AUD-009a). Найденные на проде открытые секреты — AUD-014. Выкатанный образ `stonks-server:368a00b` старше этих правок |
| AUD-009a | Контейнер `caddy` не имеет ни одного ограничения, при том что публикует 80/443. В `docker-compose.yml` у него нет `cap_drop`, `security_opt: no-new-privileges`, `mem_limit`, `pids_limit` и `read_only` — всё это есть у `mcp` и частично у `server`. У `server` дополнительно нет `cpus:` и `read_only` (данные лежат на именованных томах, так что read-only корень возможен) | проверка на `eafa3a7`; `docker-compose.yml` | FIXED_UNVERIFIED | `86c5ef2` | `caddy` получил `cap_drop: ALL` + `cap_add: NET_BIND_SERVICE`, `no-new-privileges`, `mem_limit 512m`, `pids_limit 256`, `cpus 1.0`; `server` — `cpus 1.5`. `read_only` для caddy сознательно не ставили: он пишет состояние сертификатов вне смонтированных томов. Compose валиден. **Вступает в силу только на следующем `compose up`; смену capability у caddy надо проверить с TLS в момент выката.** |
| AUD-009b | HTTP-журнал ограничен только внутри месяца. Ротация по размеру держит base + `.1`/`.2`/`.3` (до ~256 МБ), но имя файла содержит месяц (`http-access-YYYY-MM.jsonl`), и старые месячные наборы не удаляет никто — том `stonks_state` растёт без предела. Отдельно: `rotateHTTPLogIfNeeded` удаляет `path+".4"`, которого цикл переименований никогда не создаёт — мёртвая строка (кандидат в AUD-010) | проверка на `eafa3a7`; `go/internal/httpapi/accesslog.go` httpLogPathFor / rotateHTTPLogIfNeeded | FIXED_UNVERIFIED | `d9ddfa9` | Наборы старше шести месяцев удаляются при смене активного файла. Тесты `TestHTTPLogPrunesOldMonthsAndKeepsPermissions` и `TestHTTPLogSizeRotationStaysBounded` (ровно base + 3 архива, права 0640). Мёртвое удаление `.4` убрано. |
| AUD-009c | Отказ записи HTTP-журнала обрабатывается молча и fail-open. При невозможности `MkdirAll`/`OpenFile`/`Write` (заполненный диск, снятые права) `appendHTTPLog` пишет одну строку в stderr через `log.Printf` и выходит; запрос обслуживается как обычно, запись аудита теряется без алерта, счётчика или отметки в `/readyz` | проверка на `eafa3a7`; `go/internal/httpapi/accesslog.go` appendHTTPLog | FIXED_UNVERIFIED | `d9ddfa9` | Потерянные записи считаются и отдаются в `GET /api/status` полем `accessLog`; в stderr пишется первая и каждая тысячная. Обслуживание запросов по-прежнему продолжается — верный компромисс для access-лога. Тест `TestHTTPLogFailureIsCountedNotSwallowed`. |
| AUD-010 | Мёртвые ручки/поля и лишние копии логики увеличивают поверхность ошибок | исторические full/systemic audit отчёты; repository-wide прогон на `b79f79d` | FIXED_UNVERIFIED | `9fc73b4`, `858c37b` | Удалены доказанно мёртвые `metrics.CAGR`, `live.EntryFunds`, `live.EnvBroker`; дерево приведено к `gofmt`. `tradingdate.FormatDisplay` намеренно оставлена: вызовов нет, но CLAUDE.md называет её единственным способом показать торговую дату, и её удаление приглашает inline `time.Format`, запрещённый инвариантами дат. Мёртвая строка `.4` убрана в `d9ddfa9`. Список из 10 ручек без вызова из SPA остаётся решением владельца — он не доказан мёртвым. |
| AUD-012 | Ручной `ResolveTracker` не берёт `inFlight`-guard `pollTracker`: одновременная ручная и автоматическая финализация одного трекера вызывает `recordFill` дважды, второй `InsertTrade(broker_trades)` падает на UNIQUE PK, и обработчик отказа поднимает `trackerPersistFail` — все последующие входы этого брокера блокируются `execution_unknown` до ручного сброса. Дубля строки нет (PK спасает), но торговля молча останавливается. Связано с AUD-004 (тот же recordFill), причина другая | текущая проверка на `0e05089`; `go/internal/live/track.go` ResolveTracker (check-then-act по `resolvableTrackerStatus`) против inFlight-guard в `pollTracker`; `trade_record.go` InsertTrade -> raiseTrackerPersistBlock | FIXED_UNVERIFIED | `f471084` | Повторная запись уже занесённого филла больше не считается отказом: `insertJournalRow` проверяет, что строка на месте, и не поднимает `trackerPersistFail`. Переход в терминальный статус и так был CAS в `SetOrderTrackerStatus`, поэтому проигравший останавливается до логов и Telegram — guard на `ResolveTracker` не понадобился (первая попытка блокировала оператора на время обычного опроса и была отменена). Тест `TestAUD004ResolveTrackerRacesPoll` — 5/5 под `-race`. |
| AUD-013 | Остановка процесса во время T-1 сообщается как «возможно отправленная» заявка. `placeMarket` не проверяет `w.ctx.Err()` перед первой попыткой: при уже отменённом контексте отправка падает, `orderLanded` тоже падает на том же контексте и возвращает `queryFailed`, что даёт `Ambiguous` + `order_submit_status_unknown` + Telegram «Статус заявки неизвестен» и блокировку следующего входа. Ничего отправлено не было. Ambiguous корректен, когда контекст отменён во время запроса; отмена ДО старта попытки — другой случай. Связано с AUD-005 | текущая проверка на `32b1ae6`; `go/internal/live/autotrade.go` placeMarket/orderLanded, `deadline.go` deadlineExceeded (сверяет только deadline, не ctx) | FIXED_UNVERIFIED | `de2ffe7` | `placeMarket` проверяет `w.parentCtx().Err()` до старта попытки: уже отменённый контекст даёт обычный неотправленный отказ с `order_submit_aborted` вместо `Ambiguous`. Отмена во время запроса по-прежнему разрешается в `Ambiguous`, что верно. Тест `TestAUD005StopDuringT1LeavesNoOrder` с брокером, честно уважающим `cfg.Ctx` и `OrderDetailCtx`. |
| AUD-014 | Секреты в открытом виде на проде: устаревший каталог `/home/ubuntu/server.backup.1755401966/` содержит `.env` с правами `-rw-r--r--` (644), в котором лежат `ADMIN_PASSWORD`, `TELEGRAM_BOT_TOKEN`, `ALPHA_VANTAGE_API_KEY`, `FINNHUB_API_KEY` — читаемо любым пользователем хоста и любым контейнером, которому смонтирован домашний каталог. Рядом `/home/ubuntu/backups/stonks_backup_20250826_231026.tar.gz` (52 МБ, полный дамп БД) с правами `-rw-rw-r--` (664) в каталоге `drwxrwxr-x`. Оба созданы до текущего `backup-from-server.sh`, который делает всё правильно (`umask 077`, `mkdir -m 700`, `chmod 600`) | проверка прода 2026-09-06 по прямому разрешению пользователя; `ubuntu@146.235.212.239` | FIXED | — (изменение на сервере, не в репозитории) | Права поправлены на проде 2026-09-06 по прямому указанию пользователя (только `chmod`, ничего не удалялось и не перезапускалось). В 600 переведены 10 файлов `.env`/`.env.server` в stonks-контуре (`server.backup.1755401966/{,_keep/}`, `stonks-old/{,server/}`, `stonks-static/server/{,_keep/}`, `stonks/{.env,.env.server,server/.env,backup_20250829_042616/.env}`) и 2 архива (`backups/stonks_backup_20250826_231026.tar.gz`, `stonks-backups/json-final-backup-20260318_232647.tar.gz`); каталоги `backups`, `stonks-backups`, `server.backup.1755401966` — в 700. Перед правкой проверено, что ни один работающий контейнер эти файлы не читает: секреты живого стека берутся из `stonks-config/.env` (уже был 600), из `stonks/` смонтирован только `caddy/Caddyfile`. После правки: все три контейнера healthy, `https://mktorder.com/readyz` 200, `/api/status` отвечает, `docker compose config` парсится. Ротацию `ADMIN_PASSWORD` и `TELEGRAM_BOT_TOKEN` пользователь счёл ненужной — решение владельца, зафиксировано. **Вне охвата и не трогалось:** такие же 644/664 у `.env` чужих проектов на том же хосте — `ai-news-bot`, `marzban-bot`, `macdiff`, `aurum`, `server_vpn*`, `3x-ui/cert`, `ssl/cert.pem`. |
| AUD-011 | API `BacktestMetrics` и вызывающие backtest-пути разошлись после добавления пополнений | текущая проверка на рабочем дереве перед `11f58ef` | VERIFIED | `11f58ef` | `go test ./...`, `go test -race ./...`, `go vet ./...` PASS на `11f58ef`; добавлены contribution tests |
| AUD-015 | Класс `hover:underline` у ссылки «Источник» в карточке сплитов отсутствовал в собранном `web/css/app.css`, поэтому контрактный тест `TestUXAppearanceContract/class-markup-matches-css` был красным на `59c9b00`. Пайплайна сборки Tailwind в репозитории нет: `app.css` — зафиксированный артефакт, рукописные правила живут в `extra.css` | находка этого прохода на `59c9b00`; `go/web/js/app.js:5306`, `go/internal/httpapi/ui_appearance_test.go:113` | FIXED | `fade8f1` | Правило добавлено в `extra.css` рядом с существующим `.pc-add:hover`. `go test ./internal/httpapi/ -run TestUXAppearanceContract` PASS; полный прогон зелёный на `1293315` |
| AUD-016 | PATCH дня календаря при ошибке `GetCalendar` игнорирует отказ и сохраняет почти пустой документ, затирая праздники, короткие дни и покрытие брокера | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/internal/httpapi/server.go:1180-1233`; аудит на `0f4f6b9` | VERIFIED | `c9e445c` (отказ чтения), `157cc4b` (нечитаемый JSON) | Половина с ошибкой `GetCalendar` закрыта раньше в свипе AUD-002 (`c9e445c`, тест `TestAUD002UnreadableCalendarDoesNotOverwriteIt`). Вторая половина оставалась: `json.Unmarshal` не удался → `cal = map[string]any{}` → `SaveCalendar` поверх всех праздников. `157cc4b` отвечает 500 и на ошибку разбора, и на `null`. `TestAUD016CorruptCalendarIsNotOverwrittenByPatch`: битый blob с `Independence Day`, PATCH 25 декабря → 500, строка не изменилась; FAIL на предфиксном коде (200, праздник стёрт), PASS после. `go test ./...` на TZ=Pacific/Auckland и TZ=America/Los_Angeles чисто. Не задеплоено |
| AUD-017 | T-1 post-exit оркестрация читает showcase-решение Webull из `Decision`, а не фактические решения по брокерам из `BrokerDecisions`; повторный вход может ждать/не ждать fill не того брокера | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/internal/live/autotrade.go:278`, `execute_all.go:20-107`, `telegram.go:42-56`; аудит на `0f4f6b9` | VERIFIED | `dd65903` (частично), `787c337` | `787c337` вводит `effectiveDecision`: решение, по которому реально действовали, берётся из `BrokerDecisions` (выход приоритетнее входа), а showcase-решение остаётся фолбэком только когда пропущены все брокеры — тогда сохраняется поведение `dd65903`. На него переведены обе точки: `runT1Orders` (обе ветки) и `appendExec` в отчёте T-1. `TestT1WaitsForTheExitOfTheBrokerThatActuallyExited` — двухброкерный сценарий: позиция и выход на Robinhood, Webull пуст; на предфиксном коде `waitFill=false` (showcase `action=none`, fill не ждали), после фикса `waitFill=true` и SELL ровно на Robinhood. `TestT1TextReportsTheBrokerDecisionNotTheShowcase` — на предфиксном коде отчёт вообще не содержал строки об исполненном выходе. Оба FAIL до фикса, PASS после; `go test ./...` на TZ=Pacific/Auckland и TZ=America/Los_Angeles чисто. **Пробел:** `ev.Executed` остаётся агрегатом по всем брокерам — при выходе, который не отправился, и входе, который отправился, признак исполнения завышен; путь при этом деградирует в ветку повтора выхода (`t1_exit_rejected_retry`), а не в повторный вход. Не задеплоено |
| AUD-018 | Расчёты Stocks/EMA/options не передают таблицу сплитов; EMA/options также не вызывают `splits.AdjustOHLC`, поэтому raw OHLC даёт неверные сделки | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/web/js/app.js:4530-4646`, `go/internal/httpapi/calc.go:71-142`, `go/internal/backtest/{ema,options}.go`; аудит на `0f4f6b9` | FIXED | `35f0c66` | Исправлено на сервере, а не в payload SPA: таблица сплитов живёт в БД, поэтому её больше не надо присылать. `httpapi/calc.go` `pendingSplits`/`splitsFor` — явные сплиты запроса в приоритете, иначе хранимые события датасета, который ещё не пересчитан (`adjusted_for_splits = 0`); отказ чтения таблицы даёт 500, а не молчаливый расчёт по raw. Подключено во всех ручках, читающих бары: `barsWithSplits` (no-stop-loss, indicators, buy-hold), `tickersOrOne` (single-position, ema-zone, options-multi), `calcClean` (clean-backtest/backtest). `RunMultiOptions` теперь зовёт `IndexTickers` — единственный движок, который сплиты не применял (`RunSinglePosition`/`RunEmaZone` уже применяли). Тесты доказаны падающими на старом коде: `TestCalcAppliesStoredSplitsWithoutRequestSplits`, `TestCalcCleanAppliesStoredSplits`, `TestCalcBuyHoldAppliesStoredSplits` (191 вместо 95.5 без фикса), `TestRunMultiOptionsAppliesSplits` (206 вместо 103). `TestCalcSkipsSplitsOnAdjustedDataset` фиксирует, что уже пересчитанный датасет не корректируется дважды. `go test ./...`, `TZ=Pacific/Auckland`, `TZ=America/Los_Angeles` — зелено на `35f0c66`. Не задеплоено |
| AUD-019 | Повреждённый JSON настроек маскируется под дефолты без ошибки, а частичный `SetSettingsKeys` может затереть исходный blob | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/internal/store/db.go:906-908,985-992`, `go/internal/live/autotrade.go:26-51`; аудит на `0f4f6b9` | VERIFIED | `157cc4b` | Тот же корень, что AUD-016, на настройках; закрыт в его сквозной проверке. `SettingsErr` возвращает ошибку вместо дефолтов, `SetSettingsKeys` — вместо слияния на пустую карту; `Settings()` для read-only вызывающих ведёт себя как прежде. `TestAUD016CorruptSettingsAreNotOverwritten`: битый blob → `SettingsErr` ошибка, `SetSettingsKeys` ошибка, строка не изменилась; FAIL на предфиксном коде, PASS после. Не задеплоено |
| AUD-020 | Публичный `GET /api/trading-calendar` скрывает отказ БД, подменяя его `DefaultCalendarJSON` и вычисленными днями; UI не видит источник как недоступный | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/internal/httpapi/server.go:272-274,1110-1121`, `go/web/js/app.js:1068-1091`; аудит на `0f4f6b9` | FIXED_UNVERIFIED | `ebde2db` | `TestAUD020CalendarReadErrorIsNotHiddenByDefaults`: DROP TABLE calendar → GET /api/trading-calendar 500 без вычисленных праздников; на предфиксном коде 200 с полным календарём. SPA: `state.cal.error` даёт красную плашку «Календарь недоступен» вместо амбер-заметки «не импортирован». Сквозной проход по тому же корню (эхо `Settings()`/`ListSplits`/`GetDataset` из хвоста AUD-002) закрыт в том же коммите: `GET`/`PUT /api/settings` и выбор провайдера в refresh идут через `SettingsErr`; `GET /api/datasets/{id}`, `/metadata`, `detectSplitHints` и три ручки записи сплитов отвечают 500 вместо пустого списка; refresh не отдаёт пустое `to` из неудавшегося перечитывания. Тесты `TestAUD020SettingsReadErrorIsNotAnsweredWithDefaults`, `TestAUD020UnreadableSplitsAreNotEchoedAsEmpty` — FAIL до, PASS после. `go vet` и `go test ./...` (в т.ч. TZ=Pacific/Auckland) зелёные. Не задеплоено, ручной проверки UI не было |
| AUD-021 | `CanSubmit` и showcase `Evaluate().Decision` завязаны на Webull, хотя `executeAll` исполняет по каждому брокеру; UI может показывать ложный статус | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/internal/live/autotrade.go:201-216,278`, `go/internal/httpapi/server.go:1897`; аудит на `0f4f6b9` | VERIFIED | `f5d0256` (CanSubmit), `b236ad1` (витрина Decision) | Две половины. Флаг `running`: `f5d0256` перевёл `CanSubmit` на обход `brokerSnapshot()` с `brokerFlags` + `brokerHasWorkingToken`; сценарий закрыт `TestCanSubmitSeesARobinhoodOnlySetup` (Robinhood-only, `CanSubmit()` = true). Витрина `Evaluate()`: `b236ad1` — книга берётся у `showcaseBroker(cfg)` (первый включённый брокер в порядке снапшота webull→robinhood→прочие, фолбэк `defaultBroker`), а не у жёсткого `"webull"`; новое поле `decisionBroker` в `EvalResult` (и `one.DecisionBroker = name` в `executeAll`) называет брокера, чья книга дала `Decision`, а SPA (`web/js/app.js:2743`) берёт витринное решение только когда `decisionBroker === kind`, иначе — прочерк вместо чужого решения. Тест `TestShowcaseDecisionFollowsTheOnlyAttachedBroker` (Robinhood держит AAPL, webull отцеплен): доказан падающим на предфиксной строке `booksFor("webull", e.defaultBroker(), ...)` — `OpenTrade` пуст, решения о выходе нет; PASS после. `TestShowcaseDecisionStaysWebullWhenBothAreEnabled` фиксирует, что двуброкерная установка витрину не меняет. `go vet ./...`, `TZ=Pacific/Auckland go test ./...`, `TZ=America/Los_Angeles go test ./...` — зелено на `b236ad1`. Не задеплоено; ручной проверки UI на живом Robinhood не было |
| AUD-022 | Контейнер `mcp` получал общий `stonks-config/.env` с торговыми секретами через `env_file` | `docs/audits/AUDIT_2026-09-06.md` §Находки; исторический `docker-compose.yml`; аудит на `0f4f6b9` | NOT_APPLICABLE | `271da08` | YouTube MCP-сервис и его `env_file` полностью удалены коммитом `271da08`; текущий `docker compose config --services` содержит только `server` и `caddy`, поэтому исходный риск больше не существует в текущем составе проекта |
| AUD-023 | Актуализация глотает ошибку `ListWatches`/`ListEMAAlerts` и продолжает как будто тикеров нет, поэтому дневные бары мониторинга не обновляются под видом «нечего обновлять». Тот же корень в хвосте AUD-002: `ema.go:26` (`EvaluateEMAAlerts` → пустой список → пропуск всей отправки T-11/T-1 как `no_watches`), `telegram.go:704,728` (`openMonitorTrade`, история сделок) | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/internal/live/actualize.go:116,120`; аудит на `0f4f6b9` | FIXED | `7bccf9d` | Оба чтения fail-closed: `watchlist_read_failed` / `ema_alerts_read_failed`, `Success=false`. `EvaluateEMAAlerts` возвращает ошибку, агрегация T-11/T-1 отвечает `journal_unavailable` вместо `no_watches`. `openMonitorTrade` возвращает ошибку: T-1 пишет «Позиция: неизвестна (журнал сделок недоступен)», T-11 — «⚠️ Журнал сделок недоступен» и `?` вместо `FLAT` у тикеров, строка расхождений показывает `?`, когда снапшот вообще без ключей журнала; `/trades` отвечает «Журнал сделок недоступен» вместо «Нет сделок». Тесты `aud023_reads_test.go` (DROP TABLE telegram_watches / telegram_ema_alerts / trades) — FAIL на предфиксном коде (`no_tickers`, «Позиция: нет», FLAT), PASS после. `go test ./...` на `7bccf9d` зелено, кроме предсуществующего `TestVanillaUIAssets` (сломан в `95e55cf`, к находке отношения не имеет); TZ=Pacific/Auckland и TZ=America/Los_Angeles на `live`/`scheduler`/`store` зелено. Не задеплоено. Остаются NEEDS_RECHECK из того же прохода (разобраны чтением кода, fail-safe по направлению): `ema.go:89`/`telegram.go:665` `ListSplits` и `telegram.go:658` `GetOHLCLast` — без данных `EvaluatePriceIntegrity` блокирует больше, а не меньше; `actualize.go:64` `GetOHLC` — отказ даёт более широкое окно догрузки; `scheduler.go:586` `GetCalendar` — отказ ведёт к повторному импорту, не к пропуску; `scheduler.go:517` `T1ExecutionFinished` — отличается только формулировка алерта; `store/robinhood.go:16` — намеренная обёртка над `GetRobinhoodOAuthErr` |
| AUD-024 | Ручные `ClosePosition`/`TestBuy` не ставят трекер для `Ambiguous`-отправки с уже выданным `ClientOrderID`, поэтому повторный клик может отправить второй MARKET | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/internal/live/autotrade.go:739-741,1227-1231`, `execute_all.go:243-247`; аудит на `0f4f6b9` | FIXED | `7230654` | Условие журналирования перенесено в сам `startTracking`: `Ambiguous` с выданным `ClientOrderID` считается отправленным наравне с `Submitted`, поэтому трекер ставят все пути сразу — авто и ручной. Дублирующая ветка `execute_all.go` с самодельным `OrderResult{Submitted: true}` убрана, поведение автопути не изменилось. Тест `TestManualAmbiguousOrderIsTracked` (таймаут на place + таймаут на OrderDetail → Ambiguous; трекер обязан появиться, второй `ClosePosition` обязан упасть в `pending_exit_tracker_exists` без второго MARKET) FAIL на предфиксном коде, PASS на `7230654`. `go test ./...` PASS в TZ=Pacific/Auckland и TZ=America/Los_Angeles. **Проверка класса проблемы по проекту:** `SaveOrderTracker`/`TrackSubmitted` вызываются только из `startTracking`; `placeMarket` — только из `submitEvaluated` и `manualOrder`, оба идут через исправленное условие; прямых `Broker.PlaceMarket`/`ClosePosition` в обход движка нет (`internal/httpapi` зовёт только `Engine.ClosePosition`); `robinhood.CallToolCtx` не переигрывает `place_equity_order`/`cancel_equity_order` после re-auth. **Пробел:** хендлер `POST /api/webull/close-position` при `Ambiguous` отвечает `success: true` (сам `result.ambiguous` в теле есть) — отдельно от этого корня, не трогал. Не задеплоено |
| AUD-025 | Ошибка `CancelOrder` логируется и игнорируется, после чего ручной вход всё равно отправляет MARKET рядом с оставшейся рабочей заявкой | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/internal/live/autotrade.go:1348-1355`, `execute_all.go:207-217`; аудит на `0f4f6b9` | FIXED | `cc707e1`, `d433154` | Неудачная отмена своей рабочей заявки — теперь ошибка (`ErrOpenOrderCancelFailed`), а не запись в лог: `cancelOpenOrdersBeforeEntry` прекращает обход и возвращает её, `submitEvaluated` блокирует вход с причиной `open_order_cancel_failed` (отказ чтения OpenOrders по-прежнему даёт `open_orders_unavailable`). Тем же фиксом закрыт второй вход в ту же дыру: `store.IsOwnOrder` отдавала `false` при ошибке самого запроса к `order_trackers`, то есть непрочитанный журнал выглядел как «заявка чужая» — теперь возвращает `(bool, error)`, и ошибка блокирует вход. Тест `TestEntryBlockedWhenOwnOrderCancelFails` (своя рабочая заявка в OpenOrders + `CancelOrder` 503: ни одного MARKET, причина `open_order_cancel_failed`) доказан падающим на предфиксном коде — старый код отправлял BUY. `MemoryBroker.FailCancel` добавлен под этот сценарий. `TZ=Pacific/Auckland go test ./...` и `TZ=America/Los_Angeles go test ./...` — зелено на `d433154`. Не задеплоено |
| AUD-026 | Пустой/неразборчивый ответ Webull `OrderDetail` мапится в `ErrOrderNotFound`, после чего цикл отправки чеканит новый `ClientOrderID` и может дублировать уже принятый BUY | `docs/audits/AUDIT_2026-09-06.md` §Находки; `go/internal/live/autotrade.go:583-585,712-716`, `webull_broker.go:328-330`; аудит на `0f4f6b9` | FIXED_UNVERIFIED | `40f1541` | Корень: отсутствующий у брокера id Webull отдаёт HTTP- или бизнес-ошибкой, `doRequest` превращает её в обычную ошибку, поэтому ветка `ErrOrderNotFound` срабатывала **только** на 2xx с нечитаемым телом — то есть ровно там, где отсутствие не доказано. Теперь это `ErrOrderUnavailable`: `orderLanded` даёт `queryFailed`, отправка останавливается на Ambiguous, poll трекера уходит в listing-lag и затем `execution_unknown` вместо `terminal_absent`. Тесты `TestAUD026EmptyOrderDetailIsUnavailableNotAbsent` и `TestAUD026LostPlaceWithEmptyDetailDoesNotResend` (сценарий аудита: потерянный ответ Place + пустой Detail) доказаны падающими на предфиксном коде — старый код слал вторую заявку. `TZ=Pacific/Auckland go test ./...` и `TZ=America/Los_Angeles go test ./...` чисто. Живой Webull по-прежнему не дёргался: форма пустого тела взята из кода, не из ответа биржи. Не задеплоено |
| AUD-026a | Тот же корень на чтениях состояния, которые пускают вход: `LiveBroker.positions`/`openOrders` превращали 2xx-тело без единого списка в пустой срез, а `RobinhoodBroker` глотал ошибку JSON и не собирал ни строки из ответа, который не смог разобрать. И то и другое неотличимо от «позиций нет, рабочих заявок нет» — состояния, при котором `t1BrokerReconcile` пропускает вход, и вторая позиция встаёт поверх уже открытой | этот проход на `40f1541`; `go/internal/live/webull_broker.go` positions/openOrders, `go/internal/live/robinhood_broker.go` PositionsCtx/ordersByState/orderHistory/OrderDetailCtx | FIXED_UNVERIFIED | `4978ce2` | Пустой счёт всё равно отвечает списком (`{"data":[]}`, `{"data":{"holdings":[]}}`), поэтому отсутствие списка — непрочитанная страница, а не пустота: теперь ошибка, и вызывающие (`retryBrokerReadWindow` → `open_orders_unavailable`/`broker_positions_unavailable`) блокируют отправку. Тест `TestAUD026UnreadableStateReadsAreNotEmpty` (таблица: пустой список — успех, тело без списка и пустое тело — ошибка) и `TestAUD026RobinhoodUnreadableToolAnswerIsNotFlat`, оба доказаны падающими на предфиксном коде (старый ответ — `[]` без ошибки). Полный `go test ./...` в двух TZ чисто. Не задеплоено |
| AUD-026b | Тот же корень на старте процесса: `NewWithProviders` брал `db.GetRobinhoodOAuth()` (ошибка чтения глотается, см. AUD-002 `store/robinhood.go:16`) и при отказе получал нулевую строку — ровно то же, что «не подключено». Брокер не подключался на всё время жизни процесса, его позиции и рабочие заявки не попадали ни в один T-1 reconcile, и вход уходил так, будто там ничего не держат. Это уточняет пометку AUD-002 «торговля блокируется»: она верна для `brokerHasWorkingToken`, но не для точки подключения | этот проход на `4978ce2`; `go/internal/httpapi/server.go:83` | FIXED_UNVERIFIED | `30d080e` | При ошибке чтения брокер подключается, и его собственные вызовы падают громко (`open_orders_unavailable` блокирует вход) вместо невидимой книги. Тест `TestAUD026UnreadableRobinhoodRowStillAttachesBroker` (таблица `robinhood_oauth` удалена до `New`); на предфиксном коде брокер оставался неподключённым. Не задеплоено |

| AUD-027 | T-11 и T-1 срабатывали на минуту раньше своего имени, а заголовок T-1 был захардкожен. Окна `[10,12]`/`[0,2]` при тике раз в 20 c отдаются первому попавшему в них тику, поэтому сводка уходила на T-12, а решение и ордера — на T-2, по котировкам на минуту старше тех, что называют ground rules; шапка при этом всегда писала «1 минута до закрытия» | находка прохода T-11/T-1 2026-09-06 на `0f4f6b9`; `go/internal/scheduler/scheduler.go` (окна тика и `runTelegramAggregation`), `go/internal/live/telegram.go` (`Aggregate`, `buildT1Text`) | VERIFIED | `2d90743` | Симуляция дня 20-секундными тиками через настоящий `RunTick`: до — 15:48:07 ET «12m» и 15:58:07 ET решение; после — 15:49:07 ET «11m» и 15:59:07 ET. Верхние границы сужены до 11 и 1, нижние (они и терпят опоздавший тик) не тронуты; заголовок берёт минуту из данных. `TestTickT1WindowStartsAtOneMinute` (until=2 не ставит, until=1 ставит) FAIL на `0f4f6b9`, PASS на `ec99307`; заменил `TestTickT1Until2Places`, который фиксировал прежнее поведение |
| AUD-028 | Расхождение времени закрытия внутри Go: `scheduler.IsShortDay` падает на вычисляемые короткие дни для года, которого нет в календаре, а `Engine.sessionCloseMin` читал только карту. Сид-календарь знает короткие дни лишь до 2027, поэтому с 2028 планировщик вёл T-11/T-1 к закрытию 13:00, а движок отвечал 16:00: шапка сообщения врала, а `t1Deadline` выдавал три лишних часа вместо двух минут, то есть защита дедлайна T-1 (P1-1/AUD-005) молча выключалась. Родственник AUD-006c, но там паритет Go↔SPA, здесь Go↔Go | находка прохода T-11/T-1 2026-09-06 на `0f4f6b9`; `go/internal/live/telegram_t11.go` `sessionCloseMin`, `go/internal/scheduler/scheduler.go` `IsShortDay` | VERIFIED | `1d4f978` | Общий `tradingdate.IsComputedShortDay` для планировщика и движка. Замер до фикса на 2028-11-24 (пятница после Дня благодарения): планировщик `short=true close=780`, движок `closeMin=960 short=false`. `TestSessionCloseFallsBackToComputedShortDay` FAIL на предфиксном коде (`close=960 short=false`), PASS на `ec99307`. **Пробел:** на настоящем коротком дне не проверялось; в проде обычно спасает импорт `HALF_DAY` из Webull, дыра — на БД без импорта |
| AUD-029 | T-11 объявлял `🔔 ENTRY` и тег ENTRY по `nearEntry`/`nearExit` — порогу, расширенному на 0.02, — то есть обещал вход, который строгий `ibs.IsEntrySignal` на T-1 затем правомерно не делал. Это форма инцидента 2026-09-04 («T-11 сказал ENTRY, T-1 промолчал») со стороны T-11; ground rules требуют от сводки назвать тикер, который был бы выбран | находка прохода T-11/T-1 2026-09-06 на `0f4f6b9`; `go/internal/live/telegram_t11.go` `buildT11Text`, `go/internal/live/telegram.go` `evalWatch` | VERIFIED | `aec0c28` | Тег — строгий сигнал, строка ENTRY — `bestEntryRow` (наименьший IBS строго ниже своего порога), `nearDelta`/`nearEntry`/`nearExit` удалены (других потребителей не было). Строка ENTRY показывается и при открытой позиции: `runT1Orders` в одном цикле делает выход и повторный вход. `TestT11AnnouncesOnlyTheStrictSignal` FAIL на предфиксной логике, PASS на `ec99307`. Связан с AUD-017: тот же класс — объявляем решением то, что решением не станет |
| AUD-030 | `JobLog` планировщика не имел потребителя в проде: `cmd/server/main.go` передавал `onEvent = nil`, а других потребителей типа в коде нет. Пропуски, `marker-save-failed`, детали `tick-panic` и `calendar-coverage-expired` были видны только там, где рядом стоял свой `log.Printf` или уходил Telegram | находка прохода T-11/T-1 2026-09-06 на `0f4f6b9`; `go/cmd/server/main.go:48`, `go/internal/scheduler/scheduler.go` | FIXED_UNVERIFIED | `ad80fb0` | Добавлен `schedulerLog`: отсеивает то, что повторяется каждые 20 c (`tick`, `order-trackers`, детали `already-ran`/`coverage-ok`/`non-trading-day`), остальное пишет в лог. `TestSchedulerLogKeepsFailuresAndDropsChatter` PASS. **Пробел:** сам процесс не запускался, проводка `StartWith(..., schedulerLog)` подтверждена только чтением диффа и сборкой; тест против предфиксного кода невозможен — функции не существовало |
| AUD-031 | Пустой список наблюдения давал ложный «Пропущен T-11». `Aggregate` возвращал `Reason: no_watches` не отправив ничего, а `reportMissedTelegram` использовал общий текст пропуска | находка прохода T-11/T-1 2026-09-06 на `0f4f6b9`; `go/internal/live/telegram.go` `Aggregate`, `go/internal/scheduler/scheduler.go` `reportMissedTelegram` | VERIFIED | `8b573e2` | При успешно прочитанном пустом watchlist отправляется «T-11 не сформирован: Список наблюдения пуст», claim/маркер и retry после ошибки Telegram сохранены. `go test ./internal/scheduler -run 'TestMissed(T11WithEmptyWatchlistExplainsWhy|AlertRetriesAfterFailedSend)'` PASS; проверено 2026-09-06 |
| AUD-032 | `CanSubmit` — флаг «running» в UI — спрашивал только про Webull: дефолтный адаптер + `storedHealthStatus("webull")` + `TokenStatus().hasToken`. Setup только с Robinhood показывался остановленным, пока реально отправлял заявки. Тот же класс, что AUD-017: состояние одного брокера выдаётся за состояние системы | сквозная проверка класса AUD-017 по проекту 2026-09-06 на `787c337`; `go/internal/live/autotrade.go` `CanSubmit` | VERIFIED | `f5d0256` | `CanSubmit` идёт по `brokerSnapshot()` и переиспользует существующую пару `brokerFlags`/`brokerHasWorkingToken`. Тест `TestCanSubmitSeesARobinhoodOnlySetup` FAIL на предфиксном коде, PASS после. Гейтом исполнения `CanSubmit` не является (только `status.state.running` в UI), поэтому денежного эффекта у находки нет |
| AUD-033 | `RunTokenHealth` брала из результатов по всем брокерам одну строку Webull и отвечала ей за всю задачу: NEEDS_REAUTH Robinhood не попадал в `JobLog`, а в день, когда Webull уже проверен, задача писала `already-ran`, хотя Robinhood проверялся только что. Telegram-алерт при этом не терялся — он шлётся внутри каждой job. Значимость выросла после AUD-030, сделавшего `JobLog` реальным каналом оператора | сквозная проверка класса AUD-017 по проекту 2026-09-06 на `f5d0256`; `go/internal/scheduler/scheduler.go` `RunTokenHealth` | VERIFIED | `79d6037` | Строка собирается по всем брокерам (`webull=… robinhood=…`), `skipped` — только когда пропущены все; job переименована `webull-token-health` → `broker-token-health`. `TestTokenHealthCallsCheckToken` дополнен проверкой `robinhood=` и FAIL на предфиксном коде (`status=NORMAL`), PASS после |

### Обновление AUD-002 по аудиту 2026-09-06

Существующая находка `AUD-002` подтверждена на денежном пути: `awaitFlatAfterExit` игнорирует ошибку `ListTrades` и может принять отказ чтения за пустую книгу, разрешив повторный вход; `getTrade` также глотает ошибку `GetTrade`. Источник: `docs/audits/AUDIT_2026-09-06.md` §Находки, аудит на `0f4f6b9`. Статус остаётся `OPEN`; исправляющий коммит отсутствует, тест этого сценария не написан. Это обновление не создаёт новый ID.

### Обновление AUD-017 по проходу T-11/T-1 2026-09-06

Корень AUD-017 воспроизводится на одном брокере, двух не требуется: при
`storedHealthStatus = MISSING` (нет токена Webull) `executeAll` пропускает всех
брокеров, `results` пуст, `execOutcomes` не даёт ни строки, а `t1NoActionLines`
не вызывается, потому что список решений непуст. T-1 писал «Открываем AAPL по
$8.20» без единого слова об ошибке — при `execution_skipped broker=webull
reason=MISSING` и `t1_submit_failed` в `autotrade_logs`. Воспроизведено
симуляцией торгового дня через `RunTick` на `0f4f6b9`. Исправлено в `dd65903`:
пустой список исходов даёт «заявка не отправлена» плюс причину по каждому
брокеру через общий `brokerReasonLines`; `TestT1TextSaysWhenNothingWasSubmitted`
FAIL на предфиксной логике, PASS на `ec99307`. Оркестрационная часть
(`runT1Orders` читает showcase `Decision`) остаётся OPEN. Это обновление не
создаёт новый ID; родственная находка со стороны T-11 — AUD-029.


### Сквозная проверка класса AUD-017 по проекту (2026-09-06)

Класс: одно брокер-специфичное или агрегированное значение выдаётся за факт по
конкретному брокеру. Просмотрены все не-тестовые вхождения `.Decision`,
`.Executed/.Submitted/.Phase`, `.OpenTrade` и строкового `"webull"` в `go/internal`,
`go/cmd` и потребители в `go/web/js/app.js`.

Найдено и исправлено: AUD-017 (`787c337`), AUD-032 (`f5d0256`), AUD-033 (`79d6037`).

Проверено и признано корректным: `t1BrokerReconcile` и `brokerSnapshot` идут по
всем брокерам; `awaitFlatAfterExit`/`journalFlat`/`FindPendingTracker("", "exit")`
намеренно глобальны (любая открытая позиция блокирует повторный вход —
консервативно в нужную сторону); `ev.Broker` уже name-keyed (AUD-003);
`BrokersHealth` перечисляет обоих брокеров; SPA берёт `brokerDecisions[kind]` и
падает на `decision` только как на фолбэк (`app.js:2775,4392`); `broker == ""` →
`webull` в `store`/`trade_record`/`track` — задокументированное чтение legacy-строк,
а не подмена решения; ручки `handleWebullClose`/`handleWebullTestBuy` и
`WebullSummary` брокер-специфичны по своему контракту.

Не покрыто: живой broker API; сценарий «выход не отправился, вход отправился»
(завышенный `ev.Executed`, см. пробел в AUD-017).


### Сквозная проверка класса AUD-016 по проекту (2026-09-06)

Класс: нечитаемый или неразбираемый хранимый blob трактуется как пустой, и
результат пишется обратно поверх исходных данных. Половину класса — отказ
чтения — закрыл свип AUD-002 (`c9e445c`, `8887d8f`). Здесь просмотрена вторая
половина: все не-тестовые `json.Unmarshal` в `go/internal` и `go/cmd`, у
которых ошибка игнорируется (`!= nil` в условии, `_ =`, `== nil` в else-ветке).

Найдено и исправлено одним коммитом `157cc4b`:

- `httpapi/server.go` `handlePatchCalendarDay` — AUD-016, ответ 500 вместо
  пустой карты (плюс защита от `null`, который иначе паникует на записи в nil-map).
- `live/calendar_import.go` `ImportWebullCalendar` и удаление праздника — обе
  read-modify-write ветки возвращают ошибку разбора вместо `cal = {}`.
- `store/db.go` `SettingsErr` / `SetSettingsKeys` — AUD-019.
- `scheduler/scheduler.go` `ParseCalendar` — сигнатура стала
  `(Calendar, error)`; `RunTick` складывает ошибку разбора в тот же `calErr`
  (детали `calendar-read-failed`, алерт и правило «один алерт в день» уже
  были), `handlePrevDay` отвечает 500. До фикса битый календарь читался как
  ноль праздников и T-11/T-1 шли по вычисленной сессии в потенциально
  закрытый день — тест `TestAUD016CorruptCalendarSkipsMarketJobs`.

Проверено и признано корректным: `scheduler.FillComputedDays` при ошибке
разбора возвращает `raw` без изменений; `httpapi/helpers.go`
`decodeBars`/`decodeStrategy`/`decodeTrades` и `decodeSplitEvents` разбирают
пришедший в запросе payload, а не хранимое состояние; `accesslog.go` при
неразбираемом теле пишет `nil` в лог и ничего не перезаписывает; парсеры
ответов провайдеров и брокеров (`providers/client.go`, `robinhood/*`,
`webull/client.go`, `live/transport.go`) деградируют в «нет данных» по своему
контракту и в состояние не пишут; `store/db.go:785` — проверка «календарь
пуст», без записи. `calendarCoverageThrough` при битом blob даёт `""` и лишний
раз дёргает импорт — P3 из отчёта, отдельного ID не заведено (после этого
фикса RunTick до него уже не доходит).

Не покрыто: `ImportWebullCalendar` на битом blob тестом не закрыт (нужен мок
брокера); проверено чтением кода.


## Правило обновления

Перед изменением статуса сначала перечитать этот реестр, затем проверить
конкретный кодовый путь и всех его callers. Один логический фикс — один
коммит; в строку добавляются реальный короткий хэш и команда/сценарий проверки.
Исторические отчёты не удаляются и не переписываются.

### Проход по классу AUD-025 (2026-09-06, `d433154`)

Класс: **отказ шага, который устанавливает предусловие безопасности отправки
ордера, пишется в лог, а сама отправка продолжается.** Пройдены все guard-ы
пути отправки — авто (`submitEvaluated`) и ручного (`manualOrder`).

Закрыто этим фиксом: `cancelOpenOrdersBeforeEntry` — отказ `CancelOrder` и
отказ чтения `IsOwnOrder`.

Проверено и признано корректным (fail-closed уже сейчас): `t1BrokerReconcile`
(ошибка чтения OpenOrders/Positions → `skipPlace`), `submitEvaluated`
(`journal_unavailable` при отказе `AnyPendingTracker*`, `pending_tracker_exists`,
`trackerPersistBlocked` → `true` при отказе `SettingsErr`, резервация в памяти),
те же четыре проверки в `manualOrder`, `sizeOrder` (ошибка чтения счёта/позиций
и `qty<=0` на выходе — отказ), `awaitFlatAfterExit`/`journalFlat` (непрочитанный
журнал не считается «плоским»), `stampTrackerStatus` (неудачная запись
оставляет трекер в непроходном статусе, то есть блокирует), `recordFill` (все
отказы чтения/записи журнала поднимают `raiseTrackerPersistBlock`, а он ставит
флаг в памяти до попытки записи в БД). Единственный `CancelOrder` в проекте —
исправленный; прямых `Broker.PlaceMarket` в обход `placeMarket` нет.

Разошлись с классом и не трогались: `DatasetExists`, `brokerHasWorkingToken`,
`anyBrokerLive` — их отказ ведёт в безопасную сторону (404, «брокер не живой»);
`actualize.go` пишет отказ в `Failed`; дашбордные ручки отдают текст ошибки в
ответе.

Найдено этим же проходом и заведено отдельным корнем (AUD-035): записи закрытия
сделки в `recordFill` при ошибке только логировались, тогда как все чтения того
же пути поднимают persist-блок. Это не отправка лишнего ордера, а молчаливая
потеря факта выхода, поэтому отдельный ID.

| ISSUE-ID | Корень проблемы | Источники | Статус | Коммит | Проверка |
|---|---|---|---|---|---|
| AUD-035 | Отказ **записи** выхода в журнал (`closeTradeWithPnL` → `CloseTradeByID`, ветка `CloseTradePair`) только логировался, хотя все отказы **чтения** на том же пути `recordFill` поднимают `raiseTrackerPersistBlock`. Исполненный выход не попадал в журнал: позиция остаётся «открытой» там, где у брокера её уже нет, оператору об этом не говорят, а `awaitFlatAfterExit` навсегда видит счёт неплоским и молча теряет повторный вход того же дня | проход по классу AUD-025 на `d433154`; `go/internal/live/trade_record.go:104,294,366`, `go/internal/live/monitor.go:430` | FIXED | `f93c0a7` | `closeTradeWithPnL` возвращает ошибку; обе ветки закрытия в `recordFill` и `partial_exit_flat` поднимают persist-блок ровно как соседние отказы чтения; `applyConsistencyAction` больше не отвечает «применено» на отказавшую запись. Тест `TestExitJournalWriteFailureBlocksEntries` (открытая строка `broker_trades` + триггер SQLite `RAISE(ABORT)` на `UPDATE`, то есть чтения живы, а закрывающая запись отвергнута) доказан падающим на предфиксном коде. `TZ=Pacific/Auckland go test ./...` и `TZ=America/Los_Angeles go test ./...` — зелено на `f93c0a7`. **Пробел:** повторный SELL и без этого отсекался `sizeOrder` по нулевой позиции у брокера — денежного дубля тут не было и нет, чинилась именно тишина и потерянный повторный вход. Не задеплоено |

### Проход по классу AUD-018 (2026-09-06, `35f0c66`)

Класс: **хранимая на сервере поправка к ценам (таблица сплитов + флаг
`adjusted_for_splits`) доезжает до вычисления только если её прислал клиент.**
Пройдены все места, где бары попадают в расчёт цен.

Закрыто этим фиксом: тринадцать ручек `/api/calc/*` (те, что читают бары —
`clean-backtest`, `backtest`, `single-position`, `options-multi`, `ema-zone`,
`no-stop-loss`, `indicators`, `buy-hold`) и `RunMultiOptions`.

Проверено и признано корректным: `RunSinglePosition` и `RunEmaZone` применяли
сплиты через `IndexTickers` и раньше — им не хватало только самих событий;
`CalculateEmaDeviation` вызывается уже после `IndexTickers`; `/api/calc/margin`
получает бары целиком из тела запроса и из SPA не вызывается;
`/api/calc/split-adjust` — сам предпросмотр корректировки, работает по явным
событиям запроса; `metrics`, `black-scholes`, `ibs-signals` баров не читают.
Живой контур (`live/ema.go`, `live/telegram.go`) хранимую таблицу читает и при
расхождении **блокирует** сигналы через `EvaluatePriceIntegrity` — своя,
корректная защита того же класса; `live/actualize.go` берёт из баров только
последнюю дату.

Осталось незакрытым (заведено как AUD-034): пересчитанный датасет плюс **новый**
сплит.

| ISSUE-ID | Корень проблемы | Источники | Статус | Коммит | Проверка |
|---|---|---|---|---|---|
| AUD-034 | Новый сплит по уже пересчитанному датасету не применяется нигде: `adjusted_for_splits` — один булев флаг, он не говорит, какие события уже вшиты в цены. `handleRefreshDataset` кладёт новое событие через `UpsertSplits` и флаг не снимает, поэтому `pendingSplits` (по контракту «не корректировать дважды») отдаёт пусто, и бэктест считает по смешанным базисам | проход по классу AUD-018 на `35f0c66`; `go/internal/httpapi/server.go:868-871` (refresh), `go/internal/httpapi/calc.go` pendingSplits, `go/internal/store/db.go` dataset_meta.adjusted_for_splits | FIXED | `6226332` | Закрыто вариантом «помнить, что уже вшито» (выбор владельца, 2026-09-06), но точнее, чем датой: отметка `applied` лежит на самом событии в таблице `splits`, поэтому заведённый задним числом **старый** сплит тоже остаётся pending — с одной датой «пересчитано по» он бы потерялся. Схема 3 → 4: колонка `splits.applied`, миграция переносит прежний смысл (`applied=1` всем событиям датасетов с `adjusted_for_splits=1`). `pendingSplits` читает `ListPendingSplits` вместо связки «флаг + весь список»; `apply-splits` пересчитывает только непринятые события и ставит отметку, повторный вызов ничего не делает; импорт (`savePayload` с `adjustedForSplits:true`) помечает события применёнными; `ReplaceSplits`/`UpsertSplits` сохраняют отметку для неизменённых событий и сбрасывают её при смене фактора, поэтому пересохранение списка из вкладки «Сплиты» не даёт двойной корректировки. Тесты: `TestCalcAppliesSplitRegisteredAfterTheRecount` (доказан падающим: вход 95.5 вместо 47.75), `TestApplySplitsIsIdempotent`, `TestSplitListRewriteKeepsTheAppliedMark`, `TestOpenUpgradesSchemaVersion3MarksAppliedSplits`. `go vet ./...`, `go test ./...`, `-race` по `store`/`httpapi`, `TZ=Pacific/Auckland`, `TZ=America/Los_Angeles` — зелено на `6226332`. Живой контур не трогали: `EvaluatePriceIntegrity` по-прежнему смотрит на весь список и блокирует сигналы. Не задеплоено. Исходная запись находки: не воспроизводилось прогоном; найдено чтением кода. Живой контур этот случай ловит (`EvaluatePriceIntegrity` → `adjusted_dataset_split_gap`, сигналы блокируются), расчётный — нет. Честного автоматического ответа из одного флага не существует: нужна либо дата последней корректировки в `dataset_meta`, либо отказ считать (как в live), либо снятие флага при появлении нового события в refresh — решение за владельцем |

### Повторная проверка класса AUD-018 (2026-09-06, `5217702`)

AUD-018 закрыт ещё на `35f0c66` и на текущем HEAD держится: `TestCalcAppliesStoredSplitsWithoutRequestSplits`, `TestCalcCleanAppliesStoredSplits`,
`TestCalcBuyHoldAppliesStoredSplits`, `TestCalcSkipsSplitsOnAdjustedDataset`,
`TestRunMultiOptionsAppliesSplits` — зелёные. Перепройдены все места, где бары
попадают в расчёт: `barsOrDataset` (clean-backtest/backtest, split-adjust),
`barsWithSplits` (no-stop-loss, indicators, buy-hold), `tickersOrOne`
(single-position, ema-zone, options-multi), `barsForSymbol`. Хранимая таблица
доезжает везде, где должна; `/api/calc/split-adjust` по-прежнему работает по
явным событиям запроса — это предпросмотр самой корректировки.

Ничего другого сервер к расчёту не досылает: остальные поля `calcReq`
(стратегия, плечо, параметры EMA/опционов) — это состояние формы, у них нет
серверного источника истины, так что класс «хранимое на сервере доезжает только
если прислал клиент» в расчётном контуре исчерпывается сплитами.

Новая находка того же прохода — AUD-036, дефект внесён самим фиксом AUD-018.
AUD-034 закрыт следом (`6226332`): по решению владельца выбран вариант «помнить,
что уже вшито в цены». Класс AUD-018 закрыт целиком.

| ISSUE-ID | Корень проблемы | Источники | Статус | Коммит | Проверка |
|---|---|---|---|---|---|
| AUD-036 | `tickersOrOne` отдавал `req.Splits` **каждому** тикеру многотикерного прогона. Сплиты верхнего уровня относятся к одному символу (`req.Ticker`), поэтому событие AAPL 4:1 задним числом делило цены всех остальных тикеров списка: неверные входы, размер позиции и весь PnL. Внесено фиксом AUD-018 (`35f0c66`), до него ветка вообще не применяла сплиты. Из SPA не достижимо (payload прогона не несёт `splits`), достижимо через API | повторный проход по классу AUD-018 на `02988d1`; `go/internal/httpapi/calc.go` tickersOrOne | FIXED | `5217702` | Явные сплиты применяются только к тикеру, названному в `ticker`; остальные тикеры берут свою хранимую таблицу. Сплиты без `ticker` в многотикерном запросе — 400, а не молча выброшенные события. Тесты `TestCalcDoesNotApplyOneTickersSplitsToAnother` (на предфиксном коде вход по BBB шёл по 95.5 вместо сырых 191) и `TestCalcRejectsSplitsWithoutTickerOnMultiTickerRun` (на предфиксном коде 200) доказаны падающими. `go vet ./...`, `go test ./...`, `TZ=Pacific/Auckland`, `TZ=America/Los_Angeles` — зелено на `5217702`. Не задеплоено |
| AUD-037 | MCP-контейнер и server находились в одной сети Compose, поэтому MCP мог обращаться к `server:3001` напрямую, минуя Caddy | историческая проверка на `5e0236b`; `docker-compose.yml` до удаления MCP; аудит на `0f4f6b9` | NOT_APPLICABLE | `271da08` | MCP-контейнер удалён коммитом `271da08` вместе с его Compose-сервисом и маршрутом reverse proxy. Текущая эффективная конфигурация содержит только `server` и `caddy`; отдельного MCP-контейнера и межсервисного доступа MCP больше нет |

### Разбор контура актуализации EMA-тикеров (2026-09-06, `7c51902`)

Проверка вопроса «обновляется ли история EMA-тикеров». Контур подтверждён:
отдельного датасета EMA нет, `Actualize` объединяет `ListWatches` и
`ListEMAAlerts` в один список ([`actualize.go:104-127`](../../go/internal/live/actualize.go)),
`EvaluateEMAAlerts` считает по сохранённым барам плюс текущая котировка.
AUD-023 подтверждён закрытым: оба чтения фейлятся закрыто. Новых дублей нет,
одна новая находка ниже.

| ISSUE-ID | Корень проблемы | Источники | Статус | Коммит | Проверка |
|---|---|---|---|---|---|
| AUD-038 | Частичный сбой актуализации закрывал день целиком. `recordActualizeAttempt` ставил `lastActualizationDate` при `out.Count > 0`, поэтому одного успешного тикера хватало, чтобы все последующие тики того же дня отвечали `already_ran_today`. Упавшие тикеры — в том числе EMA-only, которых нет в обычном мониторинге, — оставались без дневных баров до ручного «Обновить цены и позиции»; лимит попыток не спасал, он проверяется после `lastActualizationDate`. Родственно AUD-023 по последствию (история молча не обновляется), но корень другой: там глоталась ошибка чтения, здесь частичный результат выдавался за полный | разбор контура EMA 2026-09-06 на `7c51902`; `go/internal/live/actualize.go:148-215` | VERIFIED | `f1bf1b3` | День закрывается только при `Count > 0 && len(Failed) == 0`; иначе тик повторяется в пределах `actualizationMaxAttemptsPerDay` и на исчерпании уходит алерт со списком упавших тикеров. `TestActualizePartialFailureKeepsRetrying`: AAPL в мониторинге с барами, EMA-алерт на TQQQ без истории → `Count=1`, `Failed=[TQQQ]`; на предфиксном коде FAIL («partial run must not close the day»), после фикса PASS и второй тик не отвечает `already_ran_today`. `go test ./...` чисто на `TZ=Pacific/Auckland` и `TZ=America/Los_Angeles`. Не задеплоено |

### Аудит торговой логики (2026-09-07, `43ba6cb`)

Полный проход по торговому контуру: `ibs`, `indicators`, `backtest`, `splits`,
`metrics`, `live`, `scheduler`, запись сделок в `store`. Отчёт —
[`AUDIT_2026-09-07.md`](AUDIT_2026-09-07.md). Открытых находок прошлых проходов
на входе не было; дублей не найдено, четыре новых корня ниже. Все четыре исправлены по отдельной просьбе после аудита
(`cd124fd`, `a91fac3`, `7a6d52d`, `01077ea`).

| ISSUE-ID | Корень проблемы | Источники | Статус | Коммит | Проверка |
|---|---|---|---|---|---|
| AUD-039 | Вход возможен по тикеру вне списка наблюдения. `decideLiveAction` использует `symbols` только для проверки на пустоту, а кандидатов перебирает по `quotes`; `EvaluateWindow` добавляет в `quotes` символы всех открытых строк `broker_trades` (в том числе другого брокера) и символ live-позиции. Для такого символа пороги берутся глобальные, `evalWatch` даёт `ok=true` — и он выигрывает выбор «наименьший IBS». Нарушает правило CLAUDE.md «инструмент выбирается из списка наблюдения» | `docs/audits/AUDIT_2026-09-07.md` §AUD-039; `go/internal/live/autotrade.go` `decideLiveAction`, `EvaluateWindow` | FIXED | `cd124fd` | Ветка входа перебирает только символы из `symbols` (выход по внесписочному символу не тронут — позицию надо закрывать). `TestEntryCandidateStaysInsideWatchlist`: при `symbols=["QQQ"]` и котировках QQQ (IBS 0.05) / XYZ (IBS 0.02) выбирается QQQ; котировки только по XYZ дают `none`. На предфиксном коде первый случай отдавал XYZ. `go test ./...` зелено на `TZ=Pacific/Auckland` и `TZ=America/Los_Angeles`. Не задеплоено |
| AUD-040 | `CloseTradePair` не проверяет `exitPrice > 0`, в отличие от `CloseTradeByID`, поэтому выход с неподтверждённой ценой закрывает обе строки журнала по цене 0 и пишет `pnl_percent ≈ -100`. `recordFill` (ветка `exit`) передаёт `fillPrice` без проверки; для входа тот же файл цену 0 намеренно не пишет. Достижимо при терминальном `filled` без цены в детали заявки и через `ResolveTracker(outcome="filled")` с необязательным `filledPrice` | `docs/audits/AUDIT_2026-09-07.md` §AUD-040; `go/internal/store/db.go` `CloseTradePair`/`TradeCloseFields`; `go/internal/live/trade_record.go` `recordFill` | FIXED | `a91fac3` | В `CloseTradePair` добавлен тот же гард `exitPrice > 0`, что и в `CloseTradeByID`; отказ поднимается до `recordFill`, который уже ставит `raiseTrackerPersistBlock` — путь стал симметричен одиночному. `TestCloseTradePairRejectsNonPositiveExitPrice`: цена 0 → ошибка, обе строки остаются `open`. `go test ./...` зелено в обеих зонах. Не задеплоено |
| AUD-041 | `pnl_absolute` живого журнала — разница цен на одну акцию, количество не учитывается (`TradeCloseFields`: `round6(exitPrice - entryPrice)`), хотя `quantity` в тех же строках заполнена. SPA печатает это поле как деньги: `app.js:2653` под заголовком «PnL, $» рядом с колонкой «Кол-во», `app.js:1599` в таблице сделок монитора, `app.js:4070` в CSV. Для 10 акций денежный результат занижен ровно в 10 раз. Бэктест не затронут — там свой `types.Trade.PnL` | `docs/audits/AUDIT_2026-09-07.md` §AUD-041; `go/internal/store/db.go:1362`; `go/web/js/app.js:1599,2653,2675,4070` | FIXED | `7a6d52d` | `TradeCloseFields` умножает разницу цен на `quantity` (неизвестное количество → 1 акция, прежнее поведение); `CloseTradePair` дочитывает `quantity` в своём SELECT. `TestCloseTradePairPnLAbsoluteUsesQuantity` (10 акций x $10 = $100 в обеих таблицах); ожидания `TestCloseTradeByIDBrokerTable` и `TestExitFillWritesPnLBySymbol` исправлены с поштучной разницы на деньги. `go test ./...` зелено в обеих зонах. Исторические строки пересчитаны миграцией схемы 5 (`0146f5c`): `UPDATE ... SET pnl_absolute = ROUND(pnl_absolute * quantity, 6) WHERE status='closed' AND pnl_absolute IS NOT NULL AND quantity > 0` по обеим таблицам, внутри той же транзакции `migrateSchema` и идемпотентно по версии. `TestOpenUpgradesSchemaVersion4ScalesPnLAbsolute`: строка на 10 акций → x10, на 1 акцию и без количества не тронуты, открытая с NULL не тронута, второй `Open` не умножает повторно. Не задеплоено |
| AUD-042 | Валидацию порогов watch обходит строковое значение. `validateWatchThresholds` читает пороги через `body["lowIBS"].(float64)`, а `watchHasThresholdKey` проверяет только наличие ключа, поэтому строка проходит как «поле не задано»; `UpsertWatch` → `watchFloatOrDefault` → `asFloat` её парсит и сохраняет. Глобальные пороги в `sanitizeAutoTradingConfig` защищены, дыра только в per-watch пути | `docs/audits/AUDIT_2026-09-07.md` §AUD-042; `go/internal/httpapi/server.go` `validateWatchThresholds`; `go/internal/store/db.go` `UpsertWatch`/`asFloat` | FIXED | `01077ea` | `validateWatchThresholds` требует JSON-число: присутствующий порог не типа `float64` отвергается с 400 (`lowIBS must be a number`) вместо пропуска как «поле не задано». Действует на обоих вызовах — POST и PATCH. `TestValidateWatchThresholdsRejectsNonNumbers`: строки отвергнуты, валидная пара принята, перевёрнутая пара по-прежнему отвергается. `go test ./...` зелено в обеих зонах. Не задеплоено |

### Проверка находок аудита торговой логики (2026-09-07, `c1be71c`)

Отчёт стороннего прохода — [`AUDIT_2026-09-07_TRADING_LOGIC.md`](AUDIT_2026-09-07_TRADING_LOGIC.md),
девять заявленных корней AUD-043..AUD-051. Каждый проверен по коду перед правкой:
восемь подтвердились и исправлены, AUD-048 не воспроизводится. Дублей с
AUD-039..AUD-042 нет — общих корней и путей не найдено.

| ISSUE-ID | Корень проблемы | Источники | Статус | Коммит | Проверка |
|---|---|---|---|---|---|
| AUD-043 | P1. `awaitFlatAfterExit` считала счёт плоским по всему журналу (`store.OpenBrokerTrade`, `FindPendingTracker` без брокера). В мультиброкерском режиме открытая позиция другого брокера превращала исполненный выход в сорвавшийся: `t1_exit_rejected_retry`, повторная отправка выхода и пропуск same-day re-entry | `docs/audits/AUDIT_2026-09-07_TRADING_LOGIC.md` §AUD-043; `go/internal/live/track.go` `awaitFlatAfterExit`/`journalFlat`; `go/internal/live/telegram.go` `runT1Orders` | FIXED | `8c1565d` | Проверка плоскости и поиск pending-трекера идут по брокерам, чьи решения в этом прогоне были `exit` (`exitingBrokers`); пустой список — прежняя неограниченная проверка. `TestJournalFlatIsScopedToExitingBroker`: при открытом SPY у Robinhood `journalFlat(["webull"])` = true, `journalFlat(["robinhood"])` и `journalFlat(nil)` = false. `TestExitingBrokersListsOnlyExits`. `go test ./...` зелено на `TZ=Pacific/Auckland` и `TZ=America/Los_Angeles`. Не задеплоено |
| AUD-044 | P1. Частичное исполнение выхода только уменьшало `quantity` открытой строки (`reduceOpenQuantity`): закрытая сделка на проданную часть не создавалась, реализованный PnL по этим акциям не попадал в журнал никогда | `docs/audits/AUDIT_2026-09-07_TRADING_LOGIC.md` §AUD-044; `go/internal/live/trade_record.go` `reduceOpenQuantity` | FIXED | `ac6b9e9` | Новый `store.SplitCloseTrade` вставляет закрытую строку на проданное количество с PnL из `TradeCloseFields` и уменьшает остаток — обе записи в одной транзакции, чтобы половина операции не потеряла и не удвоила акции. `TestPartialExitKeepsRemainderOpen` расширен: из 5 акций продано 2 по $11.5 при входе $10 → остаток 3 открыт, закрытая строка на 2 акции с `pnlAbsolute = 3`. `go test ./...` зелено в обеих зонах. Не задеплоено |
| AUD-045 | P1. `cancelOpenOrdersBeforeEntry` читала `client_order_id`/`status`, а Robinhood отдаёт `ref_id` и `state`: его заявки получали пустой id и молча пропускались, оставаясь живыми на бирже при отправке нового входа | `docs/audits/AUDIT_2026-09-07_TRADING_LOGIC.md` §AUD-045; `go/internal/live/autotrade.go` `cancelOpenOrdersBeforeEntry`; `go/internal/live/robinhood_broker.go` | FIXED | `e50b84c` | Идентификатор берётся через `clientOrderIDOf` (знает `ref_id`), статус нормализуется в том числе из `state`. `TestOpenOrderCancelReadsRobinhoodFields`: заявка `{ref_id, state: queued}` на символе входа отменяется до отправки ордера; на предфиксном коде FAIL. `go test ./...` зелено в обеих зонах. Не задеплоено |
| AUD-046 | P2. `round6` использовала `float64(int(v*1e6+0.5))/1e6`; приведение к `int` усекает к нулю, поэтому любой отрицательный PnL уезжал на микроединицу: -100 → -99.999999 | `docs/audits/AUDIT_2026-09-07_TRADING_LOGIC.md` §AUD-046; `go/internal/store/db.go:1359` | FIXED | `6f42226` | `math.Round(v*1e6)/1e6`. `TestRound6KeepsNegativeValuesExact`: -100, -1, -10.5 и 100 остаются собой, 0.1234565 → 0.123457 симметрично для обоих знаков. `go test ./...` зелено в обеих зонах. Историю в БД не пересчитывали: искажение в шестом знаке, миграция дороже эффекта. Не задеплоено |
| AUD-047 | P2. `SimulateMargin` считала цену маржин-колла как `borrowed / (qty*(1-maintFrac))`, без свободных денег, тогда как `marginLiquidationPrice` в `single.go` вычитает `freeCapital`. Позиция с частичным плечом ликвидировалась при кратном запасе капитала — расходится с инвариантом CLAUDE.md «капитал счёта = свободные деньги + позиция» | `docs/audits/AUDIT_2026-09-07_TRADING_LOGIC.md` §AUD-047; `go/internal/backtest/margin.go:116` | FIXED | `d1fb039` | Формула приведена к `(pos.borrowed - cash) / den`. `TestSimulateMarginCountsFreeCashAgainstMaintenance`: плечо 2 при `CapitalUsagePct=10`, падение цены со 100 до 60 → ликвидаций нет; на предфиксном коде срабатывал maintenance-колл с `RemainingCapital` 9333. `go test ./...` зелено в обеих зонах. Не задеплоено |
| AUD-048 | P2 (заявлено). «`RunSinglePosition` не зовёт `replaceFinalDailyValue` при `end_of_data`, поэтому `FinalValue` расходится с последней точкой эквити и `NetProfit` на величину комиссии выхода». Не воспроизводится: `updatePortfolio` оценивает позицию как `totalCost + stockPnL - exitCommission`, то есть комиссия выхода уже вычтена в дневной переоценке, а закрытие в конце истории даёт ровно ту же сумму | `docs/audits/AUDIT_2026-09-07_TRADING_LOGIC.md` §AUD-048; `go/internal/backtest/single.go:160-190,351-399` | NOT_APPLICABLE | — | Временный тест на `c1be71c` (одна открытая позиция на конец истории, фикс-комиссия $5): `final = lastEquity = 10099.000000`, `final - initial = NetProfit = 99.000000`, расхождений нет. Тест удалён как не несущий регрессии сверх существующих. Правок не вносилось |
| AUD-049 | P2. `RunMultiOptions` не закрывала опционы, оставшиеся открытыми на последний бар: они учитывались в эквити через `openVal`, но не попадали в `trades`, поэтому `TotalTrades`, `WinRate` и список сделок в UI считались без них — в отличие от всех остальных движков | `docs/audits/AUDIT_2026-09-07_TRADING_LOGIC.md` §AUD-049; `go/internal/backtest/options.go:240-260` | FIXED | `38ad54d` | После цикла оставшиеся позиции закрываются по теоретической цене Black-Scholes последнего дня с `ExitReason = end_of_data`; `finalValue` не меняется, так как прежде та же сумма уже сидела в `openVal`. `TestRunMultiOptionsClosesOpenPositionsAtEndOfData`: вход 2026-01-05 при экспирации через 4 недели → одна сделка с `end_of_data`; на предфиксном коде 0 сделок. `go test ./...` зелено в обеих зонах. Не задеплоено |
| AUD-050 | P2. `liveConsistencyIssues` ловила только «у брокера есть позиция, журнал пуст». Открытая сделка по другому тикеру (SPY у брокера против QQQ в журнале) проходила молча, блокировка не выставлялась | `docs/audits/AUDIT_2026-09-07_TRADING_LOGIC.md` §AUD-050; `go/internal/live/monitor.go:303-329` | FIXED | `da50d49` | Добавлена проверка принадлежности журнального тикера множеству `held` с кодом `live_broker_position_symbol_mismatch`, код внесён в `blockingMismatchCodes`. `TestConsistencyReportsSymbolMismatchAgainstJournal`: SPY у брокера при открытом QQQ → находка и непустой `BlockingMismatch`; на предфиксном коде FAIL. Существующий `TestConsistencyDoesNotMismatchTwoBrokerBooks` подтверждает, что два корректных брокерских журнала по-прежнему не блокируют. `go test ./...` зелено в обеих зонах. Не задеплоено |
| AUD-051 | P2. При `EntryExecution = "nextOpen"` `RunClean` списывала деньги по завтрашнему открытию уже на шаге дня сигнала и тут же переоценивала позицию по сегодняшнему закрытию: эквити прыгала на день раньше реальной сделки | `docs/audits/AUDIT_2026-09-07_TRADING_LOGIC.md` §AUD-051; `go/internal/backtest/clean.go:78-94,161-165` | FIXED | `131f8d3` | Пока `i < position.entryIndex`, дневная стоимость возвращает капитал до входа (стоимость покупки плюс комиссия), после входа — переоценка по закрытию как раньше. `TestCleanNextOpenKeepsSignalDayEquityFlat`: первая точка эквити равна начальному капиталу; на предфиксном коде FAIL. `go test ./...` зелено в обеих зонах. Не задеплоено |
| AUD-052 | P1. `webull.ResolveInstrumentID` брал первую строку `/instrument/list` с любым `instrument_id`, не сверяя `symbol`: если API отдаст варрант/двойной листинг раньше базовой акции, боевой MARKET уходит на чужой инструмент | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-052; `go/internal/webull/client.go:408`, `go/internal/live/webull_broker.go:66`; проверка на `83675be` | FIXED | `eaeecd3`, `42be5e3` | Строка с чужим тикером пропускается; строка без тикера остаётся допустимой (фолбэк, чтобы не ломать ответы без поля `symbol`), при полном отсутствии совпадения — ошибка вместо ордера. Тест `TestResolveInstrumentIDRequiresMatchingSymbol` (warrant `AAPLW` первым → должен вернуться `inst-aapl`; запрос `MSFT` → ошибка). `go test ./...` PASS в TZ=Pacific/Auckland и TZ=America/Los_Angeles. Не задеплоено |
| AUD-053 | P1. `RobinhoodBroker.CancelOrder` подставлял клиентский `ref_id` в параметр `order_id` инструмента `cancel_equity_order`, который по спецификации требует серверный UUID ордера (`docs/mcp/robinhood-tools.live.json:253`). Предвходовая отмена в `cancelOpenOrdersBeforeEntry` гарантированно падала и через `ErrOpenOrderCancelFailed` блокировала вход на Robinhood | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-053; `go/internal/live/robinhood_broker.go:311`, `go/internal/live/autotrade.go:1408-1422`; проверка на `83675be` | FIXED | `42be5e3` | `CancelOrder` сперва находит ордер по `ref_id` через `OrderDetail` и берёт серверный `id`; без валидного UUID — явная ошибка, а не отмена вслепую. Тест `TestRobinhoodCancelUsesBrokerOrderID` (ref ≠ id; в `cancel_equity_order` обязан уйти серверный id). Полный прогон в обеих TZ PASS. Не задеплоено |
| AUD-054 | P2. Поле `RobinhoodBroker.account` читалось и писалось без синхронизации: хендлер дашборда с `?refresh=1` зовёт `ResetAccount()` из своей горутины, планировщик в это же время резолвит аккаунт в `agenticAccountCtx` | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-054; `go/internal/live/robinhood_broker.go:21,341-358`, `go/internal/httpapi/robinhood.go:96`; проверка на `83675be` | FIXED | `ef07b38` | Кэш закрыт `sync.Mutex` через `cachedAccount`/`setAccount`; мьютекс не удерживается на время MCP-вызова (худший случай — повторный резолв, не гонка). `go test -race ./internal/live ./internal/httpapi` PASS. Отдельного конкурентного теста нет — прежний код гонку под `-race` тоже не воспроизводил, обнаружение по чтению кода |
| AUD-055 | P2. `SplitCloseTrade` вставляла закрытую часть без `is_test`/`is_hidden`/`entry_decision_time`: по умолчанию колонок — `0`, поэтому частично закрытая тестовая сделка становилась боевой и портила реальную доходность журнала, а скрытая — видимой | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-055; `go/internal/store/db.go:1590-1616`, `go/internal/live/trade_record.go:387`; проверка на `83675be` | FIXED | `64ed2e6` | Три колонки добавлены в `INSERT ... SELECT`. Тест `TestSplitCloseKeepsTestAndHiddenFlags` доказан падающим на предфиксном коде (`entry_decision_time` NULL, флаги 0) и PASS на `64ed2e6`. **Пробел:** `filled_qty`, `broker_order_id`, `client_order_id`, `linked_broker_trade_id` в закрытую часть по-прежнему не копируются — отдельный вопрос сверки с брокером, не трогал |
| AUD-056 | P2. `robinhoodQuote` возвращала `current: 0` с `err == nil`, когда инструмента нет в ответе, тогда как Finnhub/Webull/TwelveData на том же условии дают HTTP 404 (`client.go:419,526,692`). Ноль уезжал в графики и в расчёт IBS | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-056; `go/internal/providers/robinhood.go:41-65`; проверка на `83675be` | FIXED | `d77b607` | Добавлен фейл-клоуз `!(cur > 0) → HTTPError{404}` по образцу остальных провайдеров. Отдельного теста нет; `go test ./...` PASS в обеих TZ |
| AUD-057 | P2. Текст ошибки брокера вставлялся в Telegram-сообщение «статус отправки неизвестен» без `html.EscapeString`, хотя `parse_mode=HTML`: ошибка с `<` или `&` отклоняется Telegram API, и оператор не узнаёт о неопределённом ордере | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-057; `go/internal/live/execute_all.go:264-266`, `go/internal/live/transport.go:72`; проверка на `83675be` | FIXED | `bc92faf` | Экранирование по образцу `telegram.go:436`. Проход по пакету: других мест с сырым текстом ошибки в HTML-сообщениях нет (`grep "Ошибк"` — единственное вхождение). `go test ./...` PASS в обеих TZ |
| AUD-058 | P2. `backup-from-server.sh` копировал SQLite в WAL-режиме через `docker cp` при работающем сервере: файловая копия может поймать рваную страницу или WAL, не соответствующий основному файлу | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-058; `backup-from-server.sh:28`; проверка на `83675be` | FIXED | `447a1cd` | БД снимается `VACUUM INTO` во временном контейнере `alpine` с `--volumes-from stonks-server`; снимок проверяется `PRAGMA integrity_check` до того, как станет бэкапом, и отдаётся владельцу ssh-пользователя. Заодно удалённая часть переписана с тройного экранирования на `bash -s` с heredoc. **Проверка (локальная репетиция, прод не трогал):** том с `trading.db` в WAL и постоянно пишущим контейнером (WAL 760 КБ, `wal_autocheckpoint=0`); прогон удалённой части скрипта (строки 27–67) с `CONTAINER=aud058_server` дал архив с единственным `db/trading.db` без `-wal`/`-shm`, `integrity_check=ok`, распакованный снимок читается (`52658` строк, `journal_mode=delete`), `datasets`/`state` на месте. Отказной путь: несуществующий контейнер → выход 125, архив не создаётся, `scp` не отрабатывает пустоту. `bash -n` чисто. На проде не запускалось |
| AUD-059 | P2 (заявлено). «`deploy.sh` не перезапускает Caddy при изменении конфигурации». Скрипт вообще не входит в контур Caddy: он не копирует `caddy/Caddyfile` на VPS и по описанию в `CLAUDE.md` выкатывает только образ сервера | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-059; `deploy.sh` (упоминаний `caddy` нет); проверка на `83675be` | NOT_APPLICABLE | — | Не дефект, а зафиксированная граница деплоя: конфигурация Caddy обновляется отдельно. Расширять `deploy.sh` без прямой просьбы не стал |
| AUD-060 | P3 (заявлено). «Сырые `float64` в `finalValue`/`maxDrawdown` при бесконечности отдаются как `null`, и SPA печатает `—` вместо `∞`». Описание неверно: `writeJSON` (`server.go:241-252`) кодирует ответ в буфер, `encoding/json` на `±Inf`/`NaN` возвращает ошибку, и клиент получает HTTP 500 `{"error":"encode failed"}` целиком, а не `null` в одном поле. `finiteOrNil` применяется только внутри `MarshalJSON` метрик | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-060; `go/internal/httpapi/calc.go:130-136`, `go/internal/types/metrics_json.go`, `go/web/js/app.js:416` | NEEDS_RECHECK | — | Достижимость `±Inf`/`NaN` в `finalValue`/`maxDrawdown` (и в массивах `equity`, которые тем же способом уронили бы ответ) не доказана ни репродьюсером, ни тестом. Правку не делал: без сценария это спекулятивная санитизация в шести местах. Следующий шаг — подобрать вход, дающий не-конечное значение |
| AUD-061 | P3. `cleanup-server.sh` советовал запускать несуществующий `./auto-update.sh daemon`. Вторая половина находки (о секретах YouTube MCP в `.env.example`) неверна: упоминаний YouTube в файле нет, блок описывает `MCP_BEARER_TOKENS` в отдельном `mcp.env` и актуален | `docs/audits/AUDIT_2026-09-07_ALL_SUBSYSTEMS.md` AUD-061; `cleanup-server.sh:146`, `.env.example:26-31`; проверка на `83675be` | FIXED | `4620c63` | Строка совета удалена, `bash -n cleanup-server.sh` чисто. По `.env.example` изменений не требуется |
