# Trading Backtester Server

Express сервер для торгового бэктестера с модульной архитектурой.

## Регламент торговли IBS

- Торгуем только на закрытии торговой сессии; все входы совершаются по цене закрытия.
- За одну минуту до закрытия фиксируем актуальные значения IBS для всех наблюдаемых тикеров.
- При открытии выбираем из раздела мониторинга инструмент с минимальным IBS, при условии что показатель строго ниже 10; если подходящих тикеров нет, сделку не открываем.
- Держим позицию до полного закрытия. После выхода можно вновь открыть сделку в этот же день, если условия повторно выполняются.

---

## 🏗️ Архитектура

```
server/
├── server.js              # Точка входа (~140 строк)
├── src/                   # Модули сервера
│   ├── config/            # Конфигурация
│   │   └── index.js       # Загрузка переменных окружения
│   ├── middleware/        # Express middleware
│   │   ├── auth.js        # Аутентификация и сессии
│   │   └── rateLimiter.js # Ограничение частоты запросов
│   ├── providers/         # API-провайдеры данных
│   │   ├── alphaVantage.js
│   │   ├── finnhub.js
│   │   ├── twelveData.js
│   │   └── polygon.js
│   ├── routes/            # HTTP-обработчики маршрутов
│   │   ├── auth.js        # Логин/логаут/проверка сессии
│   │   ├── calendar.js    # Торговый календарь
│   │   ├── datasets.js    # CRUD датасетов
│   │   ├── quotes.js      # Котировки в реальном времени
│   │   ├── settings.js    # Настройки приложения
│   │   ├── splits.js      # Сплиты акций
│   │   ├── status.js      # Статус сервера
│   │   ├── telegram.js    # Telegram мониторинг
│   │   └── trades.js      # История сделок
│   ├── services/          # Бизнес-логика
│   │   ├── datasets.js    # Управление датасетами
│   │   ├── dates.js       # Расчёт дат
│   │   ├── settings.js    # Сервис настроек
│   │   ├── splits.js      # Обработка сплитов
│   │   ├── telegram.js    # Уведомления Telegram
│   │   └── trades.js      # Отслеживание сделок
│   └── utils/             # Вспомогательные функции
│       ├── files.js       # Файловые операции
│       └── helpers.js     # Общие утилиты
├── datasets/              # Хранилище датасетов (JSON)
├── splits.json            # Централизованные сплиты
├── settings.json          # Настройки приложения
├── telegram-watches.json  # Список наблюдения
├── trade-history.json     # История сделок
└── trading-calendar.json  # Торговый календарь
```

---

## Установка и запуск

```bash
cd server
npm install
npm run dev    # Разработка (nodemon)
npm run start  # Продакшен
```

Сервер будет доступен на `http://localhost:3001`

---

## Безопасность

- В продакшене обязательно задайте `ADMIN_PASSWORD` (bcrypt-хеш)
- API-ключи провайдеров задавайте через переменные окружения
- Включены заголовки безопасности через Helmet
- Rate limiting для защиты от перегрузки

---

## API Endpoints

### Аутентификация
- `POST /api/login` — Вход в систему
- `GET /api/auth/check` — Проверка сессии
- `POST /api/logout` — Выход

### Датасеты
- `GET /api/datasets` — Список всех датасетов (метаданные)
- `GET /api/datasets/:id` — Получить датасет с данными
- `POST /api/datasets` — Создать датасет
- `PUT /api/datasets/:id` — Обновить датасет
- `DELETE /api/datasets/:id` — Удалить датасет
- `POST /api/datasets/:id/refresh` — Обновить из API провайдера
- `POST /api/datasets/:id/apply-splits` — Применить сплиты

### Сплиты
- `GET /api/splits` — Все сплиты
- `GET /api/splits/:symbol` — Сплиты тикера
- `PUT /api/splits/:symbol` — Заменить сплиты тикера
- `PATCH /api/splits/:symbol` — Добавить/обновить сплиты
- `DELETE /api/splits/:symbol/:date` — Удалить конкретный сплит

### Котировки
- `GET /api/quote/:symbol` — Реальные котировки (мульти-провайдер)

### Telegram
- `GET /api/telegram/watches` — Список наблюдения
- `POST /api/telegram/watch` — Добавить тикер
- `POST /api/telegram/actualize-prices` — Обновить цены
- `POST /api/telegram/test` — Тестовое сообщение

### Торговля
- `GET /api/trades` — История сделок
- `GET /api/trading-calendar` — Торговый календарь
- `GET /api/trading/expected-prev-day` — Предыдущий торговый день

### Система
- `GET /api/status` — Статус сервера
- `GET /api/settings` — Получить настройки
- `PATCH /api/settings` — Обновить настройки

---

## Структура данных

Датасеты хранятся в `server/datasets/` в формате JSON:

```json
{
  "name": "AAPL_2024-01-15",
  "ticker": "AAPL",
  "uploadDate": "2024-01-15T10:30:00.000Z",
  "dataPoints": 1000,
  "dateRange": {
    "from": "2023-01-01",
    "to": "2024-01-01"
  },
  "data": [
    {
      "date": "2023-01-01T00:00:00.000Z",
      "open": 100.0,
      "high": 105.0,
      "low": 99.0,
      "close": 103.0,
      "volume": 1000000
    }
  ]
}
```

---

## Модули

### `src/config/`
Централизованная конфигурация через dotenv. Загружает переменные окружения и экспортирует типизированный объект конфигурации.

### `src/middleware/`
- **auth.js** — Аутентификация, проверка сессий, bcrypt
- **rateLimiter.js** — Rate limiting для API и загрузок

### `src/providers/`
Клиенты для внешних API с обработкой ошибок и rate limiting:
- **alphaVantage.js** — Alpha Vantage (основной провайдер)
- **finnhub.js** — Finnhub (реальные котировки)
- **twelveData.js** — Twelve Data (альтернатива)
- **polygon.js** — Polygon.io (сплиты, агрегаты)

### `src/routes/`
HTTP-обработчики, использующие сервисы и провайдеры.

### `src/services/`
Бизнес-логика, независимая от HTTP:
- **datasets.js** — CRUD, нормализация, миграция
- **dates.js** — Расчёт торговых дней, праздники
- **splits.js** — Обработка сплитов акций
- **telegram.js** — Отправка уведомлений
- **trades.js** — Отслеживание позиций и P&L