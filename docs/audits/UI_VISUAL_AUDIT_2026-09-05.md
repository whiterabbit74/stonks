# Визуальный аудит сайта — 2026-09-05

Статус: в работе. Аудит выполняется по текущему браузерному рендеру checkout `main`.

## Область и методика

- Маршруты: `/login`, `/data`, `/enhance`, `/stocks`, `/ema`, `/multi-ticker-options`, `/calendar`, `/split`, `/watches`, `/webull`, `/robinhood`, `/settings`.
- Состояния: пустые, заполненные и доступные после действия пользователя; вкладки, графики, таблицы, формы, модальные окна и действия.
- Ширины: 360, 768, 1024 и 1440 px.
- Для каждой находки фиксируются маршрут/состояние, точный визуальный симптом, серьёзность, доказательство и ожидаемое единое правило стиля.
- Для таблиц сделок отдельное правило: вход и выход должны быть сгруппированы в парные колонки с двумя значениями — «Цена покупки / продажи» и «IBS входа / выхода»; разрозненные колонки считаются несоответствием.

## Матрица обхода

| Маршрут | Состояния/вкладки | 360 | 768 | 1024 | 1440 | Статус |
|---|---|---:|---:|---:|---:|---|
| `/login` | вход, ошибки | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен; ошибка авторизации проверена в DOM |
| `/data` | библиотека, импорт CSV/JSON | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/enhance` | провайдер, форма загрузки | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/stocks` | summary, price, charts, equity, exposure, drawdown, trades, profit, duration, contribution, splits, buy-hold, no-stop-loss | ✅ | ✅ | ✅ | ✅ | базовый рендер + актуальные вкладки desktop |
| `/ema` | summary, price, deviation, equity, exposure, drawdown | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/multi-ticker-options` | summary, equity, price, charts, drawdown | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/calendar` | календарь, импорт, редактирование дня | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/split` | список, редактирование, удаление | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/watches` | summary, watches, EMA alerts, trades | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/webull` | overview, positions, orders, fills, journal, autotrade, monitor | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/robinhood` | connect/account/dashboard/tools | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/settings` | general, API, Telegram, interface, autotrade | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |

## Подтверждённые находки

### V-001 — таблица сделок не группирует вход и выход

- **Маршрут/состояние:** `/stocks?tickers=AAPL` → запущенный бэктест → вкладка «Сделки», desktop 1440 px.
- **Доказательство:** `output/playwright/ui-audit/stocks-trades-1440.png` и свежий DOM-снимок: отдельные заголовки «Цена входа» и «Цена выхода»; IBS находится в конце колонки «Дата входа-выхода» как строка вида `6.7% — 86.3%` и не имеет отдельной пары вход/выход.
- **Серьёзность:** P1 для единства финансового интерфейса; широкая таблица также становится тяжелее для чтения.
- **Ожидаемое правило:** одна колонка «Цена покупки / продажи» и одна колонка «IBS вход / выхода», в каждой — два явно подписанных значения в одинаковом формате на всех таблицах.
- **Статус:** исправлено в коммите `7fd3305`; после исправления проверены desktop и mobile screenshots `stocks-trades-after-1440.png` и `stocks-trades-after-360.png`.

### V-002 — мобильная нижняя навигация перегружена и дублирует смысл иконок

- **Маршрут/состояние:** `/data`, mobile 360 px до исправления.
- **Доказательство:** `output/playwright/ui-audit/data-360.png`: нижнее меню содержало 5 пунктов, включая «Данные» и «Опционы»; «Акции» и «EMA» использовали одинаковую иконку графика.
- **Серьёзность:** P1 для мобильной навигации и различимости разделов.
- **Исправление:** «Данные» и «Опционы» убраны из нижнего меню; для EMA назначена отдельная иконка `ema` со свечами и линией; сетка нижнего меню изменена на 3 колонки.
- **Проверка после исправления:** `output/playwright/ui-audit/data-mobile-after-nav.png`.
- **Статус:** исправлено, коммит `e78db2b`.

### V-003 — фиксированное нижнее меню перекрывает содержимое на мобильном

- **Маршруты/состояния:** `/calendar` и длинные формы `/ema`, `/multi-ticker-options`, mobile 360 px.
- **Доказательство:** `output/playwright/ui-audit/calendar-360.png`: строка списка праздников продолжается под фиксированным меню; аналогичное перекрытие видно в `ema-360.png` и `options-360.png`.
- **Серьёзность:** P1: часть данных/контролов визуально недоступна без дополнительной прокрутки и неясно, что именно скрыто.
- **Ожидаемое правило:** нижняя навигация не закрывает текущий контент; для длинных экранов нужен гарантированный нижний safe-area/отступ и проверка положения при прокрутке.
- **Исправление:** отступ `pb-32` сохранён поверх `safe-area-inset-bottom` через `calc(8rem + env(...))`; на `min-width: 768px` явно возвращён desktop-отступ `2rem`, чтобы fixed-навигация не перекрывала мобильный контент и не добавляла лишнее место на desktop.
- **Статус:** исправлено в коммите `d0e4a4b`; runtime-проверка прокруткой после разблокировки браузера требуется.

### V-004 — горизонтальные табы обрезаются на узких экранах

- **Маршруты/состояния:** `/settings`, `/watches`, `/webull`, `/robinhood`, mobile 360 px; `/robinhood`, desktop 1440 px.
- **Доказательство:** `settings-360.png` обрывает «Интерфейс», `watches-360.png` обрывает «EMA», `webull-360.png` обрывает «Исполненные», `robinhood-360.png` обрывает «Позиции»; `robinhood-1440.png` обрывает правые вкладки даже на desktop.
- **Серьёзность:** P1: пользователь не видит полный набор доступных разделов.
- **Ожидаемое правило:** таб-лист должен горизонтально прокручиваться без обрезания текста, с видимым affordance (признаком продолжения) и автопрокруткой к активному табу; на desktop тот же компонент не должен терять правые пункты.
- **Исправление:** аналитические вкладки получили несжимаемое flex-поведение и сохраняют горизонтальную прокрутку без ужатия подписей.
- **Статус:** исправлено в коммите `7409d79`; визуальная перепроверка требуется.

### V-005 — лента категорий `/enhance` не помещается на мобильной ширине

- **Маршрут/состояние:** `/enhance`, mobile 360 px.
- **Доказательство:** `enhance-360.png`: кнопки категорий с эмодзи и подписями обрезаны по краям и читаются как фрагменты (`Все`, `Поп...`, `📈NA` и т. п.).
- **Серьёзность:** P2: фильтр доступен частично и выглядит как сломанная сетка.
- **Ожидаемое правило:** единый горизонтальный scroll-контейнер с целыми кнопками, либо перенос в несколько строк; текст каждой категории должен быть полностью читаем.
- **Исправление:** элементы категорий получили несжимаемое flex-поведение и сохраняют полную ширину подписи внутри горизонтальной прокрутки.
- **Статус:** исправлено в коммите `7409d79`; визуальная перепроверка требуется.

### V-006 — переход 640 px слишком рано ужимает карточки датасетов

- **Маршрут/состояние:** `/data`, сравнение 639/640 px.
- **Доказательство:** `data-639.png` сохраняет 2 колонки с читаемыми названиями; `data-640.png` переключается на 3 колонки, из-за чего названия вроде `American Airlines ...` и `Advanced Micro ...` обрезаются.
- **Серьёзность:** P2: пограничный breakpoint ухудшает читаемость без явной потребности в третьей колонке.
- **Ожидаемое правило:** breakpoint для 3 колонок выбирать по минимальной ширине карточки/контента, а не только по стандартному порогу 640 px; проверить также 600–700 px на реальном тексте.
- **Исправление:** карточки датасетов остаются в двух колонках до `md` (768 px); третья колонка больше не появляется на границе 640 px.
- **Статус:** исправлено в коммите `898ffcc`; визуальная перепроверка требуется.

### V-007 — аналитический tab-strip и край графика теряют содержимое

- **Маршрут/состояние:** `/stocks?tickers=AAPL` → бэктест → вкладки «Сплиты» и «Опционы», desktop 1440 px; вкладка «Цены».
- **Доказательство:** `stocks-tab-11.png` и `stocks-tab-16.png`: при переходе к дальним вкладкам слева остаётся обрезанный фрагмент соседней подписи; `stocks-tab-02.png`: последняя подпись даты на правом краю графика отображается как `Se…`.
- **Серьёзность:** P2: навигация и временная шкала выглядят обрезанными, крайние значения читаются не полностью.
- **Ожидаемое правило:** активный таб прокручивается в безопасную область с сохранением читаемого соседнего контекста; график получает правый внутренний отступ/правильный формат крайнего label без обрезания.
- **Исправление:** tablist получил запас после последней вкладки, а tab-кнопки — `scroll-margin-inline`; дублирующий `fitContent()` после `applyRange()` удалён в `area`, `richLine` и `priceChart`, чтобы сохранить rightOffset графика.
- **Статус:** tab-strip и причина прижатия крайнего label исправлены в текущем коммите; визуальная перепроверка после авторизации требуется.

### V-008 — действия в мониторинге используют текст вместо единого набора иконок

- **Маршруты/состояния:** `/data`, `/watches` → «Тикеры», «Сделки» и «EMA»; `/webull`/`/robinhood` → позиции и журнал сделок.
- **Доказательство:** в исходном рендере мониторинг, список датасетов и таблица брокерских позиций использовали текстовые кнопки для закрытия, редактирования или удаления, тогда как `/split` и часть брокерского журнала уже используют иконки.
- **Серьёзность:** P2: одна и та же операция визуально оформлена по-разному, а таблицы становятся шире.
- **Ожидаемое правило:** редактирование, удаление и закрытие — компактные иконки с `aria-label` и подсказкой; текст оставлять только для первичных действий формы.
- **Исправление:** редактирование, закрытие и удаление в мониторинге, датасетах и брокерских позициях переведены на компактные иконки с подсказками и `aria-label`; текстовые переключатели состояния сохранены.
- **Статус:** исправлено в коммитах `72b15fc`, `12afe80`, `6057ded`; визуальная перепроверка требуется.

## Требования к единому виду таблиц сделок

Проверить на `/stocks`, `/watches`, `/webull`, `/robinhood` и во всех вкладках с историей сделок:

- одна колонка «Цена покупки / продажи» с двумя строками или иным однозначным парным представлением;
- одна колонка «IBS входа / выхода» с тем же парным представлением;
- одинаковый порядок, подписи, выравнивание и формат чисел во всех таблицах;
- действия редактирования и удаления — иконками с понятными `aria-label`/подсказкой, не текстовыми кнопками.

### V-009 — сравнение без маржи встроено в стандартные карточки метрик

- **Маршрут/состояние:** `/stocks?tickers=AAPL` → запущенный бэктест с маржой, summary, desktop 1440 px и mobile 360 px.
- **Требование:** не показывать отдельный блок «Сравнение режимов», а выводить под основным значением мелкую строку вида `vs 69.8% без маржи`.
- **Исправление:** comparison-панель удалена; `metricsGrid` теперь принимает baseline и выводит сравнение внутри карточек итогового баланса, доходности, CAGR, win rate, просадки, числа сделок и профит-фактора.
- **Проверка:** DOM не содержит заголовок отдельного блока (`comparisonBlocks: 0`); визуальные доказательства `stocks-comparison-cards-360.png` и `stocks-comparison-cards-1440.png`.
- **Статус:** исправлено в коммите `45f2623`.

### V-010 — форма опционов использовала подсказку тикеров вместо стандартных пресетов

- **Маршрут/состояние:** `/multi-ticker-options`, форма параметров.
- **Доказательство:** в исходном DOM формы была отдельная пунктирная кнопка `↩ AAPL, MSFT, AMZN, MAGS`; аналогичные формы `/stocks` и `/ema` используют блок «Пресеты» с выбором, сохранением и удалением.
- **Серьёзность:** P2: одинаковый сценарий настройки стратегий оформлен по-разному, сохранение пользовательских наборов невозможно.
- **Исправление:** добавлен единый по структуре блок пресетов: выбор, удаление и сохранение текущих тикеров и параметров опционов через localStorage.
- **Статус:** исправлено в коммите `f93cfc2`; визуальная проверка после повторной авторизации требуется.

### V-011 — иконка EMA не обозначала индикаторную линию

- **Маршрут/состояние:** глобальная навигация, mobile и sidebar.
- **Доказательство:** иконка `activity` читалась как общий сигнал/пульс и не связывалась с EMA 200; отдельная иконка акций уже использует график `linechart`.
- **Серьёзность:** P2: снижается распознаваемость раздела по иконке.
- **Исправление:** добавлена иконка `ema` с мини-свечами и наложенной линией; она используется в нижней и боковой навигации, не дублируя «Акции».
- **Статус:** исправлено в коммите `295d0b4`; визуальная проверка после повторной авторизации требуется.

### V-012 — раскрытый блок «Индикаторы» занимал лишнюю высоту

- **Маршрут/состояние:** графики с панелью «Индикаторы», mobile 360 px и desktop.
- **Доказательство:** раскрытые настройки цвета, толщины и стиля использовали широкую панель и большие интервалы; на узких экранах это раздвигало меню и увеличивало перекрытие графика.
- **Серьёзность:** P2: настройка графика занимает непропорционально много места.
- **Исправление:** панель уменьшена до 14rem, сокращены внутренние отступы, интервалы, размеры цветовых образцов и сегментных кнопок; сохранены aria-подписи.
- **Статус:** исправлено в коммите `dae8c12`; визуальная проверка после повторной авторизации требуется.

### V-013 — опасные кнопки отличались от основных по базовой раскладке

- **Маршруты/состояния:** модальные окна закрытия мониторинга и подтверждения удаления; все состояния с `.btn-danger`.
- **Доказательство:** `.btn-primary` и `.btn-secondary` задавали `inline-flex`, центрирование и минимальную высоту, а `.btn-danger` оставался обычной кнопкой без общего layout-контракта.
- **Серьёзность:** P2: одинаковые действия в модальных окнах имели разную высоту и выравнивание.
- **Ожидаемое правило:** все текстовые action-кнопки используют общий layout-контракт `inline-flex`, центрирование и минимум 44 px; форма и радиус остаются едиными.
- **Исправление:** `.btn-danger` приведён к тому же layout-контракту, что `.btn-primary` и `.btn-secondary`.
- **Статус:** исправлено в текущем коммите; визуальная проверка после повторной авторизации требуется.

### V-014 — форма добавления тикера не переносила действия на мобильном

- **Маршрут/состояние:** `/watches` → вкладка «Тикеры», ширина 360 px.
- **Доказательство:** форма `#watch-form` задавала `flex` без `flex-wrap`; поле и четыре кнопки, включая длинную «Обновить цены и позиции», размещались в одной строке и могли выйти за ширину viewport.
- **Серьёзность:** P1: часть действий становилась недоступной или создавала горизонтальный overflow.
- **Ожидаемое правило:** формы с несколькими действиями на узкой ширине должны переносить controls, а поле занимать доступное место без фиксированного переполнения.
- **Исправление:** добавлены `flex-wrap`/`items-center`, а поле получило `min-w-0 flex-1 basis-40`.
- **Статус:** исправлено в текущем коммите; визуальная проверка после повторной авторизации требуется.

### V-015 — строки пресетов могли вытеснять соседние действия на мобильном

- **Маршруты/состояния:** `/stocks`, `/ema`, `/multi-ticker-options` → формы параметров, ширина 360 px.
- **Доказательство:** строки выбора и сохранения пресета содержали элементы с `w-full` рядом с кнопкой удаления/сохранения, но без `min-width: 0` и flex-сжатия.
- **Серьёзность:** P1: иконка или кнопка действия могла выйти за пределы карточки и стать недоступной.
- **Ожидаемое правило:** в flex-строке поле занимает остаток ширины (`min-w-0 flex-1`), а соседнее действие сохраняет собственную ширину.
- **Исправление:** этот паттерн применён к select и полям имени во всех трёх формах пресетов.
- **Статус:** исправлено в текущем коммите; визуальная проверка после повторной авторизации требуется.

### V-016 — поле тестового сообщения Telegram не разрешало flex-сжатие

- **Маршрут/состояние:** `/settings` → Telegram → отправка тестового сообщения, ширина 360 px.
- **Доказательство:** `#tg-test-msg` был `flex-1`, но без `min-w-0` рядом с кнопкой «Отправить тест»; для input нативная минимальная ширина могла вытеснить кнопку.
- **Серьёзность:** P2: действие отправки могло стать частично недоступным на мобильном.
- **Ожидаемое правило:** гибкое поле в строке с action-кнопкой должно иметь `min-w-0`.
- **Исправление:** добавлен `min-w-0` к полю тестового сообщения.
- **Статус:** исправлено в текущем коммите; визуальная проверка после повторной авторизации требуется.

### V-017 — часть таблиц с `min-width: max-content` не имела scroll-контейнера

- **Маршруты/состояния:** `/stocks` и `/ema` → «Длительность», «Прибыль», зоны; `/watches` → «EMA», ширина 360 px.
- **Доказательство:** таблицы `.trades` в `durationBody`, `spreadsTable` и таблице EMA-оповещений выводились без `.overflow-auto`, хотя общий CSS задаёт им `min-width: max-content`.
- **Серьёзность:** P1: таблица могла расширить страницу и вызвать горизонтальный overflow вместо локальной прокрутки.
- **Ожидаемое правило:** каждая широкая таблица должна прокручиваться внутри контейнера, не меняя ширину viewport.
- **Исправление:** все найденные таблицы обёрнуты в `overflow-auto`; для двух таблиц в grid добавлен отдельный контейнер на каждую.
- **Статус:** исправлено в текущем коммите; визуальная проверка после повторной авторизации требуется.

### V-018 — длинные подписи метрик ужимались в три колонки на мобильном

- **Маршруты/состояния:** `/stocks` и `/ema` → «Длительность»/статистика графика просадки, ширина 360 px.
- **Доказательство:** карточки и статистический блок использовали `grid-cols-3` без мобильного breakpoint; подпись «Макс. дневная просадка» и аналогичные labels занимали узкие колонки.
- **Серьёзность:** P2: значения доступны, но интерфейс выглядит сжатым и хуже читается.
- **Ожидаемое правило:** длинные текстовые metric-карточки складываются в одну колонку на мобильном и становятся многоколоночными с `sm`.
- **Исправление:** добавлен `grid-cols-1 sm:grid-cols-3` в оба блока.
- **Статус:** исправлено в текущем коммите; визуальная проверка после повторной авторизации требуется.

### V-019 — длинные модальные формы могли скрывать нижние действия

- **Маршруты/состояния:** `/watches` → редактирование сделки; `/webull`/`/robinhood` → редактирование broker-сделки; ширина 360 px.
- **Доказательство:** `.modal-card` имел только ширину и padding, без ограничения высоты и вертикальной прокрутки; формы с несколькими полями могли быть выше viewport.
- **Серьёзность:** P1: кнопки сохранения и отмены могли оказаться недоступными.
- **Ожидаемое правило:** содержимое модального окна не выходит за viewport; длинная форма прокручивается внутри карточки.
- **Исправление:** добавлены `max-height: calc(100vh - 2rem)` и `overflow-y: auto`.
- **Статус:** исправлено в текущем коммите; визуальная проверка после повторной авторизации требуется.

### V-020 — периоды в toolbar графика могли вытеснять остальные controls

- **Маршруты/состояния:** `/stocks`, `/ema`, `/options` → price chart, ширина 360 px.
- **Доказательство:** `.pc-ranges` содержал семь периодов с глобальным минимумом кнопки 44 px в одной flex-строке с «Индикаторы», экспортом и fullscreen, но не задавал `min-width: 0`.
- **Серьёзность:** P1: управляющие элементы графика могли выйти за границы viewport.
- **Ожидаемое правило:** группа периодов сжимается до доступной ширины и прокручивается горизонтально внутри toolbar; соседние controls остаются видимыми.
- **Исправление:** `.pc-ranges` получил `flex: 1 1 auto` и `min-width: 0`.
- **Статус:** исправлено в текущем коммите; визуальная проверка после повторной авторизации требуется.

### V-021 — поля входа не объявляли назначение для автозаполнения

- **Маршрут/состояние:** `/login`, мобильный и desktop runtime.
- **Доказательство:** актуальная консоль браузера сообщала, что password input не имеет `autocomplete`; markup также не задавал назначение email-поля.
- **Серьёзность:** P2: менеджеры паролей и системное автозаполнение могли не предложить сохранённые данные.
- **Ожидаемое правило:** email использует `autocomplete="email"`, пароль входа — `autocomplete="current-password"`.
- **Исправление:** добавлены оба стандартных autocomplete-атрибута.
- **Статус:** исправлено в текущем коммите; runtime-проверка формы входа уже снята на 360/768/1024/1440.

### V-022 — фильтр вкладок в настройках мог сжимать подписи

- **Маршрут/состояние:** `/settings` → «Интерфейс», список вкладок, ширина 360 px.
- **Доказательство:** кнопки `data-vis` находились в `overflow-x-auto`, но не имели `shrink-0` и `whitespace-nowrap`; flex-контейнер мог сжимать labels вместо горизонтальной прокрутки.
- **Серьёзность:** P2: названия вкладок могли стать плохо читаемыми.
- **Ожидаемое правило:** каждый элемент горизонтального списка сохраняет собственную ширину и не переносит подпись.
- **Исправление:** добавлены `shrink-0 whitespace-nowrap`.
- **Статус:** исправлено в текущем коммите; визуальная проверка после авторизации требуется.

### V-023 — названия тикеров в каталоге скрывались через `truncate`

- **Маршрут/состояние:** `/enhance` → карточки каталога, узкие карточки и мобильная ширина.
- **Доказательство:** название компании в `.ticker-card` имело класс `truncate`, поэтому длинный текст заменялся многоточием; tooltip был доступен только при наведении.
- **Серьёзность:** P2: часть названия была недоступна без дополнительного действия пользователя.
- **Ожидаемое правило:** текстовые названия карточек переносятся по словам и остаются читаемыми.
- **Исправление:** `truncate` заменён на `break-words`.
- **Статус:** исправлено в текущем коммите; визуальная проверка после авторизации требуется.

### V-024 — icon-only обновление не имело `aria-label`

- **Маршрут/состояние:** страницы с предупреждением об устаревших данных, кнопка обновления котировки.
- **Доказательство:** `#stale-refresh` имела визуальную иконку и `title`, но не имела `aria-label`, в отличие от остальных icon-only controls.
- **Серьёзность:** P2: действие было менее понятно пользователям экранных дикторов.
- **Ожидаемое правило:** каждая icon-only кнопка имеет видимую подсказку и программное имя.
- **Исправление:** добавлен `aria-label="Обновить данные"`.
- **Статус:** исправлено в текущем коммите; визуальная проверка после авторизации требуется.

### V-025 — row-actions имели квадратную форму среди круглых icon-кнопок

- **Маршруты/состояния:** `/data`, `/watches`, `/split`, `/webull`, `/robinhood` → редактирование, удаление и закрытие строк.
- **Доказательство:** общий `.icon-btn` и большинство toolbar controls используют `border-radius: 9999px`, а helper `.action-icon` для тех же icon-only действий задавал `0.5rem`.
- **Серьёзность:** P2: одинаковые компактные операции выглядели по-разному на разных страницах.
- **Ожидаемое правило:** icon-only row-actions используют тот же круглый shape-контракт, что и остальные компактные иконки.
- **Исправление:** `.action-icon` переведён на `border-radius: 9999px`.
- **Статус:** исправлено в текущем коммите; визуальная проверка после авторизации требуется.

### V-026 — desktop shell сохранял мобильный нижний отступ

- **Маршрут/состояние:** все авторизованные страницы shell, ширина от 768 px.
- **Доказательство:** `#main-content` задавал `pb-32` и `md:pb-24`, хотя `.bottom-nav` скрывается с `md`; footer дополнительно имеет `margin-top: 50px`.
- **Серьёзность:** P2: между последним блоком и footer образовывался лишний вертикальный пробел.
- **Ожидаемое правило:** большой нижний отступ нужен только под mobile fixed-nav; на desktop используется обычный page padding.
- **Исправление:** `md:pb-24` заменён на `md:pb-8`, мобильный `pb-32` сохранён.
- **Статус:** исправлено в текущем коммите; визуальная проверка после авторизации требуется.

## Промежуточная статическая перепроверка — 2026-09-05

- Все найденные таблицы в `go/web/js/app.js` проверены повторным поиском: таблицы сделок используют парные колонки «Цена покупки / продажи» и «IBS вход / выход»; отдельные поля входа/выхода остались только в формах редактирования и в таблицах наблюдаемых котировок, где выхода нет по смыслу.
- Все текущие icon-only controls в исходнике имеют `title` и `aria-label`; текстовые кнопки в строках сделок оставлены только для переключения состояния («Показать/Скрыть»), а редактирование и удаление используют иконки.
- Breakpoint-контракт проверен: mobile-only правила используют `max-width: 767px`, desktop/`md` — `min-width: 768px`; карточки каталога используют 1 / 2 / 3 / 4 колонки на 0 / 640 / 1024 / 1280 px.
- Полный `go test ./...` после последнего CSS-фикса проходит. Реальная перепроверка авторизованных заполненных состояний отложена: браузерный UI сейчас недоступен из-за заблокированного macOS.

### V-027 — компактные фильтры и действия использовали разные shape-контракты

- **Маршруты/состояния:** `/watches` → «Сделки» и `/data` → карточки датасетов.
- **Доказательство:** фильтры сделок и кнопки «Обновить/Экспорт» использовали локальные классы `rounded border`, тогда как соседние фильтры каталога используют общие `chip-on/chip-off`, а текстовые вторичные действия — `.btn-secondary`.
- **Серьёзность:** P2: одинаковые компактные controls имели разные радиусы, высоту, типографику и hover-состояния.
- **Исправление:** фильтры мониторинга переведены на `chip-on/chip-off`; экспорт и обновление датасетов — на `.btn-secondary` с компактной высотой.
- **Статус:** исправлено в текущем коммите; визуальная проверка после авторизации требуется.

### V-028 — меню действий карточки датасета имело квадратную icon-only кнопку

- **Маршрут/состояние:** `/data` → карточки датасетов, меню действий.
- **Доказательство:** кнопка «Открыть меню действий» использовала `rounded-md`, тогда как общий `.icon-btn` и row-actions используют круглый shape-контракт.
- **Серьёзность:** P2: одинаковые компактные icon-only controls выглядели по-разному.
- **Исправление:** кнопка переведена на существующий `.icon-btn` с круглой формой и тем же touch-target.
- **Статус:** исправлено в текущем коммите; визуальная проверка после авторизации требуется.

### V-029 — аварийная и управляющая кнопки выпадали из общего button-стиля

- **Маршруты/состояния:** общий экран ошибки подтверждения входа и `/stocks`/`/ema`/`/multi-ticker-options` → настройки hero-графика.
- **Доказательство:** кнопка «Повторить» использовала чёрный фон без общего `.btn-primary`; переключатель «Показывать сделки» использовал базовый `rounded` вместо единого `rounded-lg` для текстовых controls.
- **Серьёзность:** P2: одинаковые действия имели разные цветовой и shape-контракты.
- **Исправление:** «Повторить» переведена на `.btn-primary`, переключателю сделок задан `rounded-lg`.
- **Статус:** исправлено в текущем коммите; визуальная проверка после авторизации требуется.

### V-030 — компактная сетка датасетов была слишком плотной на mobile

- **Маршрут/состояние:** `/data` → компактный вид библиотеки, ширина 360 px.
- **Доказательство:** сохранённый рендер `output/playwright/ui-audit/data-360.png` показывает две узкие колонки; названия компаний вроде `American Airlines ...` и `Bank of America C...` обрезаются через `truncate`.
- **Серьёзность:** P2: текст карточек теряет смысловую часть и сетка выглядит тесной.
- **Исправление:** компактная сетка использует одну колонку на mobile и две начиная с `sm` (640 px); название компании переведено на перенос по словам.
- **Статус:** исправлено в текущем коммите; runtime-перепроверка после авторизации требуется.

### V-031 — hero-грид делился слишком рано на breakpoint 768 px

- **Маршрут/состояние:** `/stocks`, ширина 768 px, пустая форма бэктеста.
- **Доказательство:** `output/playwright/ui-audit/stocks-768.png`: после sidebar остаётся узкая рабочая область, но `hero-grid` уже использует две колонки; подписи пресета обрезаны (`Выбрать пре…`, `Название пре…`).
- **Серьёзность:** P1: важные поля формы теряют читаемость на стандартной tablet-ширине.
- **Исправление:** двухколоночный `hero-grid` включается только с `1024px`; до этого график и параметры идут последовательно в одной колонке.
- **Статус:** исправлено в текущем коммите; runtime-перепроверка после авторизации требуется.

### V-032 — callback-поле Robinhood не занимало ширину карточки

- **Маршрут/состояние:** `/robinhood` → «Подключение», ширина 768 px.
- **Доказательство:** `output/playwright/ui-audit/robinhood-768.png`: длинное поле после подписи «Вставьте адрес после разрешения» отображается в одной строке и обрезается справа.
- **Серьёзность:** P1: callback URL нельзя полностью прочитать или надёжно вставить/проверить.
- **Исправление:** input получил `w-full`, поэтому он занимает отдельную полную строку внутри label и не зависит от длины подписи.
- **Статус:** исправлено в текущем коммите; runtime-перепроверка после авторизации требуется.

### V-033 — tab-фильтр интерфейса не показывал продолжение списка

- **Маршрут/состояние:** `/settings` → «Интерфейс», ширина 768 px.
- **Доказательство:** `output/playwright/ui-audit/settings-768.png`: справа виден только обрезанный фрагмент следующей вкладки; контейнер прокручиваемый, но без визуального scroll-cue (индикатора продолжения).
- **Серьёзность:** P2: доступная вкладка выглядит потерянной и неочевидно, что список можно прокрутить.
- **Исправление:** контейнер tab-фильтра получил существующий `scroll-cue` с градиентным индикатором края; `shrink-0 whitespace-nowrap` у кнопок сохранён.
- **Статус:** исправлено в текущем коммите; runtime-перепроверка после авторизации требуется.

### V-034 — периоды hero-графика не имели scroll-cue

- **Маршрут/состояние:** `/stocks`, `/ema`, `/multi-ticker-options` → hero-график с периодами, узкая ширина.
- **Доказательство:** контейнер `1M…MAX` был горизонтально прокручиваемым, но отличался от tab-листов и категорий отсутствием визуального индикатора продолжения и запаса после последнего элемента.
- **Серьёзность:** P2: крайний период мог выглядеть потерянным или обрезанным.
- **Исправление:** контейнер периодов переведён на существующий `scroll-cue`, внутренней полосе добавлен правый запас.
- **Статус:** исправлено в текущем коммите; runtime-перепроверка после авторизации требуется.

### V-035 — удаление события сплита оставалось текстовым крестиком

- **Маршрут/состояние:** `/split` → редактирование событий тикера.
- **Доказательство:** строка события в модальном окне использовала кнопку `×` с текстовым оформлением, хотя остальные удаления используют единый trash icon.
- **Серьёзность:** P2: явное действие удаления расходилось с общим правилом icon-only controls.
- **Исправление:** `×` заменён на общий `actionIcon('trash', ...)` с tooltip и `aria-label`; атрибут `data-rm-row` сохранён.
- **Статус:** исправлено в текущем коммите; визуальная проверка после авторизации требуется.

### V-036 — навигация календаря использовала текстовые стрелки вместо общей SVG-иконки

- **Маршрут/состояние:** `/calendar` → переключение месяца.
- **Доказательство:** кнопки `cal-prev` и `cal-next` имели единый круглый `.icon-btn`, но внутри использовали глифы `‹`/`›`; остальные icon-only controls используют общий `icon()` с SVG-путями. Текстовые глифы зависят от шрифта и визуально отличаются по толщине и центровке.
- **Серьёзность:** P2: один и тот же тип навигационного действия выглядел иначе, чем sidebar и другие контролы.
- **Исправление:** текстовые глифы заменены на существующие `chevronleft`/`chevronright` через `icon()`; обработчики и доступные подписи не изменены.
- **Статус:** исправлено в коммите `e53bb9b`; визуальная проверка после авторизации требуется.

### V-037 — действия EMA-оповещений были обычными текстовыми кнопками

- **Маршрут/состояние:** `/watches` → вкладка «EMA» → строки оповещений.
- **Доказательство:** кнопки `Включено`, `Ждать покупку` и `Ждать продажу` использовали только `text-sm`, без общего чипа, скругления, компактного touch-target и поведения соседних фильтров/действий. На узкой ширине они также не имели явного flex-контейнера для переноса.
- **Серьёзность:** P2: одинаковые управляющие действия в одном продукте выглядели как неоформленный текст и могли тесниться в ячейке.
- **Исправление:** статус переведён на `chip-on/chip-off`, действия — на `chip-off`; кнопки собраны в `flex flex-wrap` с единым `rounded-lg`, внутренними отступами и размером текста.
- **Статус:** исправлено в коммите `4002af0`; визуальная проверка после авторизации требуется.

### V-038 — меню действий датасета не перерисовывалось после открытия

- **Маршрут/состояние:** `/data` → компактный вид → меню `⋯` на карточке датасета.
- **Доказательство:** обработчик переключал `state.menuTicker`, но вызывал `renderPage({ keepCharts: true })`; прежняя ветка `renderPage` при этом обновляла только chrome и не меняла `#page-root`, где меню строится из `state.menuTicker`. Поэтому меню «Изменить / Обновить / Экспорт / Удалить» не появлялось.
- **Серьёзность:** P1: доступные действия карточки были недоступны в основном сценарии.
- **Исправление:** `#page-root` теперь перерисовывается при каждом `renderPage`; флаг `keepCharts` по-прежнему управляет только вызовом `Charts.destroy()`.
- **Статус:** исправлено в коммите `d7b1d9f`; после разблокировки браузера требуется runtime-проверка кликом по `⋯`.

### V-039 — переключатель вида датасетов имел отдельный квадратный shape-контракт

- **Маршрут/состояние:** `/data` → переключатель «Список / Компактный вид».
- **Доказательство:** обе icon-only кнопки использовали `rounded` (4 px), тогда как контейнер переключателя, фильтры и остальные компактные controls используют `rounded-lg` (8 px). Это создавало визуально более квадратные кнопки внутри единого интерфейса.
- **Серьёзность:** P2: одинаковые компактные controls имели разный радиус.
- **Исправление:** кнопки переключателя переведены на существующий `rounded-lg`; размеры, доступные подписи и обработчики сохранены.
- **Статус:** исправлено в коммите `7021f29`; runtime-перепроверка после разблокировки браузера требуется.

### V-040 — обновление котировки в строке broker-таблицы оставалось текстовой кнопкой

- **Маршрут/состояние:** `/webull` или `/robinhood` → вкладка с котировками → действие строки.
- **Доказательство:** кнопка `Обновить` использовала только `text-sm text-indigo-600`, тогда как остальные строковые действия используют единый `actionIcon()` с tooltip, `aria-label` и круглым touch-target; массовое обновление в шапке остаётся текстовой кнопкой намеренно.
- **Серьёзность:** P2: одинаковое действие обновления имело разные представление и размеры в пределах одного broker-раздела.
- **Исправление:** строковая кнопка заменена на существующий `actionIcon('refresh', ...)` с сохранённым `data-bq` и тем же обработчиком.
- **Статус:** исправлено в коммите `9f941cc`; runtime-перепроверка после разблокировки браузера требуется.

### V-041 — в CSS оставались неиспользуемые альтернативные style-контракты

- **Область:** `go/web/css/extra.css` → `.nav-active` и `.auto-fields`.
- **Доказательство:** поиск по всему репозиторию не нашёл ни одного применения этих классов в SPA, HTML или `charts.js`; текущая навигация и сетки используют другие контракты.
- **Серьёзность:** P3: мёртвые правила создавали риск вернуть устаревший радиус/цвет навигации или несогласованный breakpoint при дальнейшем изменении.
- **Исправление:** неиспользуемые правила удалены; текущий рендер и активные классы не затронуты.
- **Статус:** исправлено в коммите `3cd960c`; runtime-перепроверка не требуется для удаления мёртвого CSS.

### V-042 — кнопка добавления данных в шапке имела квадратную icon-only форму

- **Маршрут/состояние:** `/data` → «Библиотека датасетов» → кнопка `+`.
- **Доказательство:** ссылка использовала локальные `w-8 h-8 rounded-lg` и не имела `aria-label`, тогда как самостоятельные icon-only controls приложения используют круглый `icon-btn`/`icon-btn-md` с единым touch-target.
- **Серьёзность:** P2: одинаковые иконографические действия имели разный shape и размер; действие также имело менее явную доступную подпись.
- **Исправление:** ссылка переведена на `icon-btn icon-btn-md icon-btn-glass` и получила `aria-label`; маршрут и действие не изменены.
- **Статус:** исправлено в коммите `bc13841`; runtime-перепроверка после разблокировки браузера требуется.

### V-043 — меню действий датасета не имело hover-подсказки

- **Маршрут/состояние:** `/data` → компактный вид → кнопка `⋯`.
- **Доказательство:** кнопка имела `aria-label`, но отсутствовал `title`; остальные самостоятельные icon-only controls используют оба атрибута для доступного имени и визуальной подсказки при наведении.
- **Серьёзность:** P3: действие было доступно скринридеру, но при наведении не объяснялось единообразно с остальными иконками.
- **Исправление:** добавлен `title="Открыть меню действий"`; обработчик, форма и размеры не изменены.
- **Статус:** исправлено в коммите `47ee6db`; runtime-перепроверка после разблокировки браузера требуется.

### V-044 — broker-таблицы смешивали русские и английские заголовки

- **Маршруты/состояния:** `/webull` и `/robinhood` → открытые ордера, история исполнений, tracked orders и мониторинг котировок.
- **Доказательство:** видимые заголовки одной русской страницы включали `Side`, `Qty`, `Filled Qty`, `Type`, `Instrument`, `Avg Price`, `Order ID`, `Open`, `High`, `Low`, `Current IBS` и `Prev Close`, тогда как соседние колонки уже были русскими.
- **Серьёзность:** P2: смешанный язык увеличивал когнитивную нагрузку и нарушал единый текстовый стиль интерфейса.
- **Исправление:** видимые заголовки переведены на русский (`Направление`, `Кол-во`, `Исполнено`, `Тип`, `Инструмент`, `Средняя цена`, `ID ордера` и т. п.); значения API и технические поля не менялись.
- **Статус:** исправлено в коммите `0ce78ea`; runtime-перепроверка после разблокировки браузера требуется.

### V-045 — выпадающее меню датасета начиналось внутри trigger-кнопки

- **Маршрут/состояние:** `/data` → компактный вид → открытое меню `⋯`.
- **Доказательство:** меню имело `top-8` (`2rem`), а trigger расположен на `top-2` и получает глобальный `min-height:44px`; нижняя граница кнопки находится ниже точки старта меню, поэтому элементы меню перекрывали область trigger.
- **Серьёзность:** P2: открытое меню визуально наслаивалось на кнопку и могло мешать повторному нажатию/восприятию границ.
- **Исправление:** меню сдвинуто на `top-14` (`3.5rem`); utility-класс добавлен в `extra.css`, чтобы позиция была определена в shipped CSS.
- **Статус:** исправлено в коммите `520d095`; runtime-перепроверка после разблокировки браузера требуется.

### V-046 — основное сохранение настроек выглядело как вторичное действие

- **Маршрут/состояние:** `/settings` → кнопка «Сохранить» в шапке.
- **Доказательство:** кнопка отправки основной формы использовала `btn-secondary`, тогда как сохранение во вложенных формах использует `btn-primary`; на странице это визуально снижало приоритет главного действия.
- **Серьёзность:** P2: основной CTA страницы имел другой цветовой уровень, чем аналогичные операции сохранения.
- **Исправление:** кнопка переведена на `btn-primary` с сохранёнными компактными размерами и атрибутом `form="set-form"`.
- **Статус:** исправлено в коммите `9c329d0`; runtime-перепроверка после разблокировки браузера требуется.

### V-047 — сравнение режима без маржи отсутствовало в карточках опционов

- **Маршрут/состояние:** `/multi-ticker-options` → запущенный бэктест с маржинальностью выше 100% → сводка.
- **Доказательство:** общий `metricsGrid()` уже поддерживал компактную подпись `vs … без маржи`, а `/stocks` и `/ema` передавали baseline. Опционная страница вызывала его без baseline; API опционов также не строит baseline самостоятельно, поэтому сравнение в этом экране не появлялось.
- **Серьёзность:** P1: пользователь не видел сопоставимый результат режима с маржой и 100% на одном из основных бэктест-экранов.
- **Исправление:** для опционов базовые сделки строятся через существующий `single-position` с `includeBaseline`, затем тем же конфигом рассчитывается baseline `options-multi`; результат передаётся в стандартные карточки метрик без отдельного блока.
- **Статус:** исправлено в коммите `a185b72`; runtime-перепроверка после разблокировки браузера требуется.

### V-048 — на русских экранах оставались несогласованные английские подписи

- **Область:** сводные карточки аналитики, модальные формы сделок и страницы брокеров.
- **Доказательство:** рядом с русскими подписями отображались `Win rate`, `Exit IBS`, `Entries / Exits`, `Cash / settled cash`, `Buying power`, `Reconcile`, `Consistency` и `Raw broker log`; это нарушало единый язык интерфейса и визуально выделяло отдельные элементы.
- **Серьёзность:** P2: смешанный язык увеличивал когнитивную нагрузку и нарушал единый стиль подписей.
- **Исправление:** пользовательские подписи переведены на русский (`Доля прибыльных`, `IBS выхода`, `Входы / выходы`, `Сверить данные`, `Согласованность` и т. п.); технические значения и идентификаторы не изменялись.
- **Статус:** исправлено в коммите `d14b02b`; runtime-перепроверка после разблокировки браузера требуется.

### V-049 — переключатели вкладок в настройках интерфейса были без общего скругления

- **Маршрут/состояние:** `/settings` → «Интерфейс» → «Вкладки страницы «Акции»».
- **Доказательство:** кнопки переключения видимости использовали `border-b-2`, но не имели `rounded-lg`, тогда как остальные chip-контролы фильтров и статусов используют единый радиус 8 px.
- **Серьёзность:** P2: одинаковый тип компактного переключателя имел различную форму.
- **Исправление:** к этим кнопкам добавлен существующий `rounded-lg`; прокрутка, подчёркивание активного состояния и поведение не изменены.
- **Статус:** исправлено в коммите `a2a1d5f`; runtime-перепроверка после разблокировки браузера требуется.

### V-050 — компактные аналитические подписи использовали непоследовательные сокращения

- **Область:** компактная сводка графика и мини-статистика графиков тикеров.
- **Доказательство:** основной блок метрик использует понятные подписи «Макс. просадка» и «Доля прибыльных», а компактная версия показывала `Макс. DD` и `Win`, из-за чего одинаковые показатели назывались по-разному в соседних представлениях.
- **Серьёзность:** P2: различающиеся подписи усложняли быстрое чтение и нарушали единый текстовый стиль.
- **Исправление:** сокращения заменены на «Макс. просадка» и «Прибыльных»; расчёты и формат значений не изменялись.
- **Статус:** исправлено в коммите `38038d3`; runtime-перепроверка после разблокировки браузера требуется.

### V-051 — статус сервера в общем chrome был на английском

- **Область:** `/data` и нижняя часть общего интерфейса с состоянием API.
- **Доказательство:** при одинаковом компоненте состояния рядом с русскими сообщениями отображались `Online` и `Offline`; статус «Проверка» уже был русским.
- **Серьёзность:** P2: один статусный компонент имел смешанный язык и разную подачу состояний.
- **Исправление:** видимые подписи заменены на «Онлайн» и «Офлайн»; внутренние значения `Online/Offline` сохранены только в `title` для совместимости с защитным UI-контрактом.
- **Статус:** исправлено в коммите `6f7e009`; runtime-перепроверка после разблокировки браузера требуется.

### V-052 — доступное имя кнопки сверки брокера не совпадало с видимой подписью

- **Маршрут/состояние:** `/webull` или `/robinhood` → «Мониторинг» → кнопка сверки.
- **Доказательство:** видимый текст кнопки был «Сверить данные», но её `aria-label` оставался `Reconcile Candidate`; пользователи скринридера слышали другое название действия.
- **Серьёзность:** P2: несогласованное доступное имя ухудшало понимание действия и нарушало единый язык интерфейса.
- **Исправление:** `aria-label` теперь начинается с видимого названия «Сверить данные», а технический термин сохранён в скобках для совместимости с существующим UI-контрактом.
- **Статус:** исправлено в коммите `a988a6c`; runtime-перепроверка после авторизации требуется.

### V-053 — статус автоторговли оставался в виде `LIVE/OFF`

- **Маршрут/состояние:** `/webull` или `/robinhood` → «Автоторговля» → карточка общего состояния.
- **Доказательство:** карточка имела русскую подпись «Статус», но значение выводилось как `LIVE` или `OFF`, тогда как соседние статусы страницы используют русские «включена/выключена».
- **Серьёзность:** P2: один и тот же статусный паттерн имел смешанный язык и разные форматы.
- **Исправление:** видимые значения заменены на «Включена» и «Выключена»; внутренний флаг `ac.enabled` и поведение не менялись.
- **Статус:** исправлено в коммите `1264600`; runtime-перепроверка после авторизации требуется.

### V-054 — в русских формах и диагностике оставались английские подписи

- **Область:** вложенные расчёты на аналитической странице и диагностические блоки брокерских экранов.
- **Доказательство:** рядом с русскими полями отображались `lowIBS`, `highIBS`, `Buy at close…`, `Buy at close 4…`, `IV adj, %`, а заголовки исходных данных были `Raw open orders payload` и `Raw monitoring payload`.
- **Серьёзность:** P2: смешанный язык нарушал единый стиль интерфейса и усложнял чтение параметров и диагностической информации.
- **Исправление:** видимые подписи переведены на русский (`Нижний IBS`, `Верхний IBS`, `Покупка на закрытии…`, `Корр. IV, %`, `Исходные данные ...`); имена полей формы и формат данных не изменялись.
- **Статус:** исправлено в коммите `741c774`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-055 — брокерские диагностические блоки смешивали русский и английский интерфейс

- **Область:** `/webull` и `/robinhood` → баланс, позиции, история ордеров, автоторговля и тестовое подключение.
- **Доказательство:** видимые заголовки оставались `Raw ... payload` и `Pending / last tracked orders`, а тестовая кнопка начиналась с `BUY`; это не соответствовало русским заголовкам и действиям рядом.
- **Серьёзность:** P2: смешанный язык снижал единообразие и усложнял чтение диагностических состояний.
- **Исправление:** заголовки заменены на «Исходные данные ...», блок ожидания — на «Ожидающие и последние отслеживаемые ордера», тестовая кнопка — на «Купить AAL...». Внутренний тестовый маркер `BUY AAL` сохранён в обработчике и UI-контракте.
- **Статус:** исправлено в коммите `7f83e2d`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-056 — в таблицах брокерского журнала оставались англоязычные заголовки идентификаторов

- **Область:** `/webull` и `/robinhood` → «Журнал сделок» и «Позиции».
- **Доказательство:** русская таблица журнала содержала `Client Order ID` и `Broker Order ID`, а таблица позиций использовала неполную подпись `Средняя` для цены.
- **Серьёзность:** P2: заголовки выбивались из общего языка и были менее понятны без знания брокерского API.
- **Исправление:** заголовки заменены на «ID заявки клиента», «ID заявки брокера» и «Средняя цена»; имена полей данных и значения API не менялись.
- **Статус:** исправлено в коммите `d7f8346`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-057 — заголовок редактора брокерской сделки смешивал языки

- **Маршрут/состояние:** `/webull` или `/robinhood` → редактирование сделки.
- **Доказательство:** заголовок модального окна содержал `Редактировать broker-сделку`, хотя остальной пользовательский интерфейс русскоязычный.
- **Серьёзность:** P2: смешанная подпись выбивалась из единого текстового стиля.
- **Исправление:** `broker-сделку` заменено на «брокерскую сделку»; логика модального окна не менялась.
- **Статус:** исправлено в коммите `b6f4aef`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-058 — таблица отслеживаемых ордеров имела два одинаковых заголовка «Действие»

- **Маршрут/состояние:** `/webull` или `/robinhood` → «Автоторговля» → заполненная таблица ожидающих ордеров.
- **Доказательство:** третий столбец выводит операцию из `o.action`, а последний — кнопки ручного разрешения; оба столбца были подписаны «Действие».
- **Серьёзность:** P2: одинаковые заголовки затрудняли понимание данных и действий в широкой таблице, особенно на мобильной горизонтальной прокрутке.
- **Исправление:** третий заголовок заменён на «Операция», последний оставлен «Действие»; данные и обработчики не менялись.
- **Статус:** исправлено в коммите `3bebc72`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-059 — пояснение закрытия мониторинга смешивало русский и английский текст

- **Маршрут/состояние:** `/watches` или брокерская страница → закрытие открытой сделки мониторинга.
- **Доказательство:** в модальном окне отображалось «нашу `monitor`-сделку» и «Webull-ордер», хотя пояснение целиком русскоязычное.
- **Серьёзность:** P2: текст действия выбивался из единого языка интерфейса и мог быть непонятен пользователю.
- **Исправление:** подпись заменена на «сделку мониторинга» и «Ордер Webull»; действие по-прежнему не отправляет брокерский ордер.
- **Статус:** исправлено в коммите `c1eaece`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-060 — статус и форма токена содержали англоязычные фрагменты

- **Область:** `/watches` → закрытие сделки; `/webull` → «Автоторговля» → подключение Webull.
- **Доказательство:** toast показывал `Monitor-сделка закрыта`, а карточка подключения — `token`, `lastCheckAt` и placeholder `Webull token` среди русских подписей.
- **Серьёзность:** P2: повторяющиеся смешанные подписи нарушали единый язык интерфейса и делали статусы менее ясными.
- **Исправление:** текст приведён к русским формулировкам «Сделка мониторинга закрыта», «Токен», «последняя проверка» и «Вставьте токен Webull»; внутренние ключи и формат данных сохранены.
- **Статус:** исправлено в коммите `77f8e9a`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-061 — footer формы входа содержал английский слоган

- **Маршрут/состояние:** `/login`, базовый публичный рендер.
- **Доказательство:** рядом с русским текстом футера отображалось `Built with ❤️ for traders`.
- **Серьёзность:** P2: публичный экран входа нарушал единый язык интерфейса.
- **Исправление:** слоган заменён на «Создано с ❤️ для трейдеров»; бренд, год и юридическая подпись не менялись.
- **Статус:** исправлено в коммите `27d5415`; живой DOM `/login` подтверждал исходный текст, после изменения пройдены синтаксическая проверка JavaScript и `git diff --check`.

### V-062 — термины мониторинга и тестовые статусы смешивали русский и английский

- **Область:** `/watches`, `/split`, `/webull` и `/robinhood` — согласованность, ручная сделка, импорт событий и журнал.
- **Доказательство:** пользовательские подписи содержали `monitor / broker`, `monitor-позиция`, `monitor-сделка`, `corp-action`, `broker-журнал` и маркер `test` рядом с русским текстом.
- **Серьёзность:** P2: одинаковые пользовательские сущности назывались разными языками и нарушали единый стиль интерфейса.
- **Исправление:** видимые формулировки приведены к «мониторинг», «сделка мониторинга», «корпоративные события», «журнал брокера» и «тест»; технические ключи API сохранены.
- **Статус:** исправлено в коммите `ccfd9f6`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка заполненных состояний после авторизации требуется.

### V-063 — в таблице сделок мониторинга оставался английский маркер тестовой записи

- **Маршрут/состояние:** `/watches` → «Сделки» → строка с тестовой сделкой.
- **Доказательство:** рядом с русским статусом сделки выводился маркер `· test`.
- **Серьёзность:** P2: смешанный язык повторял уже исправленную проблему в другом табличном представлении.
- **Исправление:** маркер заменён на `· тест`; флаг `isTest` и данные сделки не менялись.
- **Статус:** исправлено в коммите `524ff14`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-064 — статус `OK` в подключении Robinhood отображался как технический код

- **Маршрут/состояние:** `/robinhood` → «Подключение» и карточка подключения на вкладке «Автоторговля».
- **Доказательство:** при успешном состоянии API значение `status: OK` напрямую показывалось пользователю, тогда как общий helper уже переводит этот статус в «активно».
- **Серьёзность:** P2: технический английский код выбивался из русских статусов брокерских экранов.
- **Исправление:** отображение статуса подключено к существующему `brokerHealthText()` с русским fallback «подключено»; значение API `OK` не менялось.
- **Статус:** исправлено в коммите `085f020`; синтаксическая проверка JavaScript и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-065 — названия вкладок аналитики показывали внутренние английские идентификаторы

- **Маршрут/состояние:** `/stocks` → список аналитических вкладок, включая сравнение и вложенные расчёты.
- **Доказательство:** среди русских вкладок отображались `Buy & Hold`, `BuyAtClose` и `BuyAtClose4`; последние два значения выглядели как технические имена, а не как названия функций.
- **Серьёзность:** P2: смешанный язык и внутренние идентификаторы ухудшали навигацию и нарушали единый стиль подписей.
- **Исправление:** labels заменены на «Купить и держать», «Покупка на закрытии» и «Покупка на закрытии 4»; идентификаторы вкладок и обработчики не менялись.
- **Статус:** исправлено в коммите `521aa8e`; `node --check`, `git diff --check` и UI-контрактные тесты HTTP API пройдены.

### V-066 — подсказка горизонтальной прокрутки отображалась даже без overflow

- **Область:** общие tab-листы, лента категорий и диапазоны графика (`.scroll-cue`).
- **Доказательство:** CSS всегда рисовал правый градиент и добавлял запас справа для `.scroll-cue`, независимо от того, превышает ли `scrollWidth` ширину контейнера; на страницах, где все элементы помещаются, крайний пункт выглядел затемнённым.
- **Серьёзность:** P2: декоративная подсказка вводила в заблуждение и могла визуально перекрывать последний label на desktop.
- **Исправление:** добавлен общий расчёт фактического overflow; класс `scroll-cue-overflow`, градиент и дополнительный отступ включаются только при необходимости и обновляются после рендера и при resize.
- **Статус:** исправлено в коммите `04ee10c`; `node --check` и `git diff --check` пройдены, визуальная перепроверка авторизованных состояний требуется.

### V-067 — карточка согласованности показывала технический статус `OK`

- **Область:** `/watches` → «Сводка» и `/webull`/`/robinhood` → «Мониторинг».
- **Доказательство:** при отсутствии расхождений статус выводился как `OK`, а пояснение содержало `Monitor` и `broker` рядом с русским текстом.
- **Серьёзность:** P2: один и тот же результат согласованности имел технический англоязычный label вместо понятного русскоязычного статуса.
- **Исправление:** видимый статус заменён на «Согласовано», пояснения — на «журналы мониторинга и брокера»; внутреннее сравнение `consKind === 'OK'` сохранено.
- **Статус:** исправлено в коммите `632a6d7`; `node --check`, `git diff --check` и UI-контрактные тесты HTTP API пройдены.

### V-068 — сетка наборов данных нарушала высоту карточек, а иконки вида были не по центру

- **Маршрут/состояние:** `/data` → компактный вид набора данных.
- **Доказательство:** в одной строке карточки AMD/AMZN и GOOG/INTC имели разные высоты: рамка была на ссылке с высотой по содержимому внутри `.relative`, поэтому наличие компании или тега меняло геометрию сетки. Кнопки «список/сетка» использовали только padding без явного flex-центрирования, из-за чего иконки визуально смещались.
- **Серьёзность:** P1: основной экран данных выглядел сломанным и не имел единого ритма карточек; переключатель вида выглядел как набор несогласованных кнопок.
- **Исправление:** элемент сетки и ссылка получили `h-full`; кнопки видов получили одинаковые `h-11 w-11 inline-flex items-center justify-center` и нулевой padding.
- **Статус:** исправлено в коммите `c18300b`; `node --check` и `git diff --check` пройдены, визуальная перепроверка в авторизованном состоянии требуется.

### V-069 — характеристики стратегии дублировались под формой запуска

- **Маршрут/состояние:** `/stocks` и `/multi-ticker-options` → «Сводка» после расчёта.
- **Доказательство:** верхний общий блок уже показывал CAGR, просадку, долю прибыльных, профит-фактор и число сделок; правая панель через `asideExtrasHTML()` повторно добавляла тот же набор в компактном виде.
- **Серьёзность:** P2: повтор занимал место в панели параметров, визуально ломал иерархию и создавал впечатление разных наборов характеристик.
- **Исправление:** из `asideExtrasHTML()` убран только `compactMetricsHTML()`; предупреждение об устаревших данных и карточка открытой позиции сохранены.
- **Статус:** исправлено в коммите `f25c92a`; `node --check` и `git diff --check` пройдены, runtime-перепроверка после авторизации требуется.

### V-070 — градиент подсказки прокрутки уезжал вместе с вкладками

- **Маршрут/состояние:** любой длинный горизонтальный список вкладок; особенно `/stocks` → аналитические вкладки, как на приложенном скриншоте.
- **Доказательство:** затемняющий правый градиент был псевдоэлементом самого `overflow-x-auto` контейнера. При горизонтальной прокрутке он двигался вместе с содержимым и мог затемнять не правый край viewport-контейнера, а одну из вкладок.
- **Серьёзность:** P1: подсказка перекрывала содержимое и создавала ложное ощущение, что вкладка находится под неподвижной маской.
- **Исправление:** добавлена неподвижная оболочка `scroll-cue-shell`; прокрутка осталась на внутреннем `.scroll-cue`, а класс overflow синхронно выставляется на оболочку для градиента.
- **Статус:** исправлено в коммите `2458851`; `node --check` и `git diff --check` пройдены, runtime-проверка прокрутки в авторизованных вкладках требуется.

### V-071 — название вкладки «Профит Фактор» отличалось от общего стиля

- **Маршрут/состояние:** `/multi-ticker-options` → аналитические вкладки.
- **Доказательство:** вкладка опционов использовала написание «Профит Фактор», тогда как карточка метрики и вкладки EMA используют единое «Профит-фактор».
- **Серьёзность:** P2: повторяющийся термин оформлялся по-разному в соседних разделах.
- **Исправление:** label вкладки приведён к «Профит-фактор»; идентификатор и обработчики не менялись.
- **Статус:** исправлено в коммите `f5b07c7`; `node --check` и `git diff --check` пройдены.

### V-072 — светлые разделители Lightweight Charts разделяли панели графика

- **Маршрут/состояние:** `/stocks`, `/ema`, `/multi-ticker-options` → график цен с панелями объёма и IBS.
- **Доказательство:** на приложенном скриншоте видны две светлые горизонтальные полосы между ценой, объёмом и IBS. В vendor-библиотеке Lightweight Charts 5.1.0 это стандартные `layout.panes.separatorColor` и `separatorHoverColor` со светлым значением по умолчанию `#E0E3EB`.
- **Серьёзность:** P1: разделители выглядели как случайные белые полосы и ломали цельный тёмный график.
- **Исправление:** цвета разделителей панелей явно установлены равными фону текущей темы; данные, размеры панелей и возможность их настройки не менялись.
- **Статус:** исправлено в коммите `1905889`; `node --check` и `git diff --check` пройдены. Полный `charts_map_test.cjs` выявил два существующих несовпадения тестов с текущим API IBS (`ibsColoredLineData` отсутствует, второй тест ожидает старую структуру), не связанные с этим изменением.

### V-073 — кнопка «Индикаторы» выбивалась текстовым контролом из панели графика

- **Маршрут/состояние:** профессиональный график на `/stocks`, `/ema` и `/multi-ticker-options` → панель цен.
- **Доказательство:** рядом с круглыми кнопками экспорта и полноэкранного режима использовалась широкая текстовая кнопка «Индикаторы» с отдельной стрелкой; её форма и визуальный вес отличались от остальных действий панели.
- **Серьёзность:** P2: управляющий элемент выглядел неаккуратно и нарушал единый язык иконок графика.
- **Исправление:** добавлена отдельная SVG-иконка индикаторов (оси, линия и точки), кнопка переведена на общий `icon-btn` с одинаковым размером и сохранёнными `title`, `aria-label`, состоянием открытия и связью с панелью.
- **Статус:** исправлено в коммите `59c817a`; `node --check` и `git diff --check` пройдены, визуальная проверка открытой панели после авторизации требуется.

### V-074 — в списке вкладок опционов сохранялось старое написание «Профит Фактор»

- **Маршрут/состояние:** `/multi-ticker-options` → аналитические вкладки.
- **Доказательство:** после унификации подписи в одном массиве вкладок второе вхождение осталось в `OPTIONS_TABS`, поэтому пользователь всё ещё видел расхождение с `/stocks` и `/ema`.
- **Серьёзность:** P2: одинаковый показатель имел разные названия в соседних разделах.
- **Исправление:** подпись в `OPTIONS_TABS` заменена на «Профит-фактор».
- **Статус:** исправлено в коммите `0fdd231`; `node --check` и `git diff --check` пройдены.

### V-075 — кнопка «Сегодня» на календаре не соответствовала стилю действий

- **Маршрут/состояние:** `/calendar` → панель выбора месяца.
- **Доказательство:** рядом с круглыми кнопками перехода и полями выбора месяца кнопка «Сегодня» была обычной текстовой ссылкой без общей высоты, границы и фона действия.
- **Серьёзность:** P2: элемент управления выбивался из общего визуального ритма панели.
- **Исправление:** кнопка приведена к компактному стилю `btn-secondary`; обработчик и действие не менялись.
- **Статус:** исправлено в коммите `53d36d3`; `node --check` и `git diff --check` пройдены.

### V-076 — выбор диапазона цены восстанавливал старый viewport вместо выбранного

- **Маршрут/состояние:** `/stocks` → «Цены» → диапазоны `1M`, `3M`, `6M`, `1Y`, `3Y`, `5Y`, «Всё».
- **Доказательство:** обработчик менял `state.priceChart.range`, но `paintPriceChart()` перед перерисовкой сохранял текущий `getVisibleLogicalRange()` и безусловно восстанавливал его после `Charts.priceChart()`. Поэтому новый диапазон сразу перезаписывался старым.
- **Серьёзность:** P1: основное управление масштабом графика не давало видимого результата.
- **Исправление:** при выборе диапазона перерисовка вызывается с `preserveRange: false`; настройки индикаторов и прочие перерисовки по-прежнему сохраняют текущий viewport.
- **Статус:** исправлено в коммите `53d36d3`; `node --check` и `git diff --check` пройдены, runtime-проверка кликов на заполненном графике требуется.

### V-077 — текстовое действие видимости смешивалось с иконками действий сделки

- **Маршрут/состояние:** `/webull` → «Журнал сделок».
- **Доказательство:** в колонке действий «Изменить» и «Удалить» были иконками, а «Скрыть/Показать» оставалось текстовой кнопкой. Это нарушало единый паттерн действий в одной строке.
- **Серьёзность:** P2: действие работало, но визуально выбивалось из общего стиля и занимало непредсказуемую ширину.
- **Исправление:** «Скрыть/Показать» переведено на круглую иконку глаза с `title` и `aria-label`; обработчик `data-hide-bt` сохранён.
- **Статус:** исправлено в коммите `58063ca`; `node --check` и `git diff --check` пройдены.

### V-078 — заголовок колонки действий различался между таблицами брокера

- **Маршрут/состояние:** `/webull` и `/robinhood` → «Позиции», «Журнал», «Мониторинг», «Автоторговля».
- **Доказательство:** в таблицах с действиями использовались оба варианта — «Действие» и «Действия», хотя визуально это одна и та же колонка с управляющими кнопками.
- **Серьёзность:** P3: функциональность не нарушалась, но терминология и визуальная структура таблиц были непоследовательными.
- **Исправление:** заголовки и подписи пустых состояний приведены к единому «Действия».
- **Статус:** исправлено в коммите `536babd`; `node --check` и `git diff --check` пройдены.

### V-079 — контейнеры графиков имели меньший радиус, чем остальные панели

- **Маршруты/состояния:** `/stocks`, `/ema`, `/multi-ticker-options` и аналитические блоки с графиками.
- **Доказательство:** повторяющиеся контейнеры графиков использовали `chart-box ... rounded` (4 px), тогда как карточки и панели интерфейса используют `rounded-lg` (8 px); из-за этого вложенные графики визуально выбивались из общего радиуса.
- **Серьёзность:** P3: функциональность не нарушалась, но повторяющийся визуальный элемент имел другой радиус.
- **Исправление:** контейнеры графиков приведены к `rounded-lg`; размеры, прокрутка и renderer не изменены.
- **Статус:** исправлено в коммите `9f5ccf1`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-080 — мини-карточки статистики просадки имели меньший радиус

- **Маршруты/состояния:** `/stocks` → аналитические блоки просадки; `/ema` → аналитические блоки просадки.
- **Доказательство:** карточки «Макс. дневная просадка», «Точек с просадкой», «Частота» и связанные итоговые плашки использовали `rounded` (4 px), тогда как соседние панели и карточки используют `rounded-lg` (8 px).
- **Серьёзность:** P3: функциональность не нарушалась, но повторяющиеся элементы одного визуального уровня имели разные радиусы.
- **Исправление:** мини-карточки и итоговые плашки приведены к `rounded-lg`; содержимое и расчёты не изменены.
- **Статус:** исправлено в коммите `c4e106c`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-081 — контейнеры таблиц сделок имели меньший радиус

- **Маршруты/состояния:** `/stocks` → таблица сделок; `/watches` → список закрытых сделок.
- **Доказательство:** оба повторяющихся контейнера `table-wrap` использовали `rounded` (4 px), тогда как остальные крупные панели интерфейса используют `rounded-lg` (8 px).
- **Серьёзность:** P3: функциональность не нарушалась, но таблицы визуально отличались от соседних блоков.
- **Исправление:** контейнеры таблиц сделок приведены к `rounded-lg`; прокрутка и размеры таблиц не изменены.
- **Статус:** исправлено в текущем коммите; после фикса пройдены синтаксическая и UI-проверки.

### V-082 — сетка календаря могла выходить за ширину карточки на мобильном экране

- **Маршрут/состояние:** `/calendar` → календарная сетка на узких мобильных ширинах.
- **Доказательство:** глобальное правило `button:not(.compact)` задаёт минимальную ширину 44 px; семь таких ячеек плюс интервалы не помещаются в узкую карточку календаря.
- **Серьёзность:** P2: на малых ширинах календарь мог создавать горизонтальный выход контента и нарушать ширину карточки.
- **Исправление:** ячейки календаря получили локальный `min-width: 0` и `width: 100%`, сохранив минимальную высоту 44 px для удобного нажатия.
- **Статус:** исправлено в коммите `86f4e20`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-083 — строка датасета могла вытолкнуть действия за ширину карточки

- **Маршрут/состояние:** `/data` → режим списка на мобильной ширине.
- **Доказательство:** строка датасета использовала `justify-between` без переноса, а текстовый блок не имел `min-w-0`; длинное название компании или диапазон дат мог расширить его вместо сжатия.
- **Серьёзность:** P2: кнопки действий могли выйти за границы карточки или создать горизонтальный overflow.
- **Исправление:** строка получила `flex-wrap` и gap, текстовая часть — `min-w-0 flex-1`, блок действий — `shrink-0`; порядок и действия не изменены.
- **Статус:** исправлено в коммите `a631c6d`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-084 — действие добавления отклонения раздувало dropdown индикаторов

- **Маршруты/состояния:** `/stocks`, `/ema` и `/multi-ticker-options` → «Индикаторы» → настройки EMA 200.
- **Доказательство:** `.pc-add` визуально являлся компактной строкой, но глобальное правило `button:not(.compact)` задавало ему минимальные 44 px по высоте и ширине.
- **Серьёзность:** P2: выпадающий блок становился выше и менее компактным, чем остальные строки настроек индикаторов.
- **Исправление:** для `.pc-add` явно заданы `min-width: 0` и `min-height: 0`; основные кнопки управления и доступный размер клика не изменены.
- **Статус:** исправлено в коммите `e5506e1`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-085 — inline-действия разрешения ордера раздували строку таблицы

- **Маршрут/состояние:** `/webull` и `/robinhood` → «Автоторговля» → зависшие заявки.
- **Доказательство:** кнопки «Исполнено у брокера» и «Заявки нет» имели только `text-xs`, но наследовали глобальный минимум 44 px по высоте и ширине; это делало строку выше соседних строк.
- **Серьёзность:** P2: таблица имела непоследовательную высоту строк, особенно заметную на мобильной ширине.
- **Исправление:** действия переведены в компактные inline-кнопки с `min-w-0`, `min-h-0` и небольшим padding; логика разрешения заявки не изменена.
- **Статус:** исправлено в коммите `e6c2205`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-086 — проверка горизонтального переполнения таблиц брокера и сплитов

- **Маршруты/состояния:** `/webull`, `/robinhood` и `/split` → заполненные таблицы.
- **Результат проверки:** таблицы брокера и списки сделок используют `overflow-auto`; заполненный список сплитов дополнительно находится внутри прокручиваемого `#spl-list`. Длинные заголовки и идентификаторы не расширяют страницу целиком.
- **Статус:** дефект не подтверждён; код не изменялся.

### V-087 — значения в карточке автоторговли могли выходить за ширину на мобильном экране

- **Маршруты/состояния:** `/webull` и `/robinhood` → «Автоторговля» → карточка параметров исполнения.
- **Доказательство:** строки `<dt>/<dd>` внутри `broker-grid` использовали `display:flex` без `min-width: 0` и переноса длинных значений; режим капитала, резервы и описание окна исполнения могли расширять строку.
- **Серьёзность:** P2: длинные параметры могли создавать горизонтальный overflow или ломать выравнивание карточки.
- **Исправление:** строки и значения получили локальное ограничение ширины и перенос длинных слов; desktop-сетка и содержимое не изменены.
- **Статус:** исправлено в коммите `0fa5295`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-088 — выбранная карточка тикера имела другую геометрию

- **Маршрут/состояние:** `/enhance` → список тикеров; выбранный/загруженный тикер.
- **Доказательство:** обычная карточка использовала границу 1 px, а `.ticker-card.loaded` — 2 px; содержимое выбранной карточки получало другой внутренний отступ и могло менять высоту относительно соседних карточек.
- **Серьёзность:** P2: состояние выбора визуально меняло геометрию повторяющихся карточек.
- **Исправление:** граница оставлена 1 px, а дополнительный акцент перенесён во внешний `box-shadow`; цветовое выделение сохранено.
- **Статус:** исправлено в коммите `174a421`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-089 — в автоторговле пользователю показывались внутренние статусы на английском

- **Маршруты/состояния:** `/webull` и `/robinhood` → «Автоторговля» → ожидающие ордера.
- **Доказательство:** в таблице и пояснении отображались машинные коды `execution_unknown / unresolved`, хотя остальная панель и действия локализованы на русском.
- **Серьёзность:** P2: технические значения ухудшали читаемость и выбивались из единого языка интерфейса.
- **Исправление:** для отображения добавлены русские подписи «исполнение не подтверждено» и «требует ручного разбора»; внутренние коды и логика блокировки сохранены.
- **Статус:** исправлено в коммите `d04651a`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-090 — даты в журнале сделок брокера имели другой формат

- **Маршруты/состояния:** `/webull` и `/robinhood` → «Журнал сделок».
- **Доказательство:** даты входа и выхода выводились сырой строкой, тогда как таблицы `/stocks`, `/watches` и карточки сделок используют локализованный формат торговой даты.
- **Серьёзность:** P3: одна и та же сущность отображалась в разных форматах в соседних разделах.
- **Исправление:** даты журнала переведены на общий `fmtTradingDate()`; сортировка, значения и API-данные не изменены.
- **Статус:** исправлено в коммите `a44b6b7`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

### V-091 — значения направления и статуса ордера оставались техническими кодами

- **Маршруты/состояния:** `/webull` и `/robinhood` → «Ордера», «Исполненные», «Автоторговля».
- **Доказательство:** русские заголовки таблиц соседствовали с сырыми значениями `BUY/SELL`, `FILLED`, `PARTIALLY_FILLED` и другими кодами API.
- **Серьёзность:** P2: ключевые значения таблицы были непоследовательны по языку и хуже читались пользователем.
- **Исправление:** направления и распространённые статусы получили единые русские подписи; машинные значения, фильтрация и API-контракт сохранены.
- **Статус:** исправлено в коммите `bfe000f`; `node --check`, UI/appearance-тесты и JavaScript-тесты графиков пройдены.

## Ограничения

- До авторизации и без тестовых данных часть заполненных таблиц и графиков может быть недоступна; такие места будут помечены отдельно, а не объявлены проверенными.
- Скриншоты текущего обхода сохраняются в `output/playwright/` и используются как доказательство находок.
- Для `/stocks` после запуска AAPL-бэктеста открыты все 16 вкладок; остальные страницы сняты в базовом состоянии без разрушительных действий и без изменения пользовательских данных.
