# 🚀 Система Развертывания и Обслуживания

> **Важно:** вне зависимости от сценария развертывания соблюдайте единый регламент торговли IBS: сделки открываем только на закрытии, данные IBS фиксируем за одну минуту до конца торгов, выбираем из мониторинга тикер с минимальным IBS ниже 10, удерживаем позицию до полного закрытия и при необходимости можем повторно войти в этот же день после выхода.

## Проблемы, которые решает эта система

### 🔴 Основные проблемы предыдущей системы:
1. **Старый код на сервере** - Docker volume монтирование перезаписывало новые файлы старыми
2. **Отсутствие мониторинга** - Нет проверки здоровья системы
3. **Рискованные обновления** - Нет возможности отката при неудаче
4. **Ручная очистка** - Накопление старых образов и файлов
5. **Отсутствие автоматизации** - Все действия выполнялись вручную

### ✅ Решения новой системы:
1. **Надежные обновления** - Умный деплой с проверками и откатом
2. **Мониторинг здоровья** - Постоянная проверка всех компонентов
3. **Безопасные откаты** - Однокомандный возврат к рабочей версии
4. **Автоматическая очистка** - Удаление старых файлов и образов
5. **Автообновления** - Автоматическое применение обновлений из GitHub

## 📋 Компоненты системы

### 1. 🚀 `deploy.sh` - Умное развертывание
```bash
# Развертывание с проверками и откатом
./deploy.sh
```

**Что делает:**
- ✅ Проверяет синтаксис (предотвращает TypeScript в JS ошибки)
- ✅ Создает бэкап текущего состояния
- ✅ Очищает старые Docker образы
- ✅ Пересобирает и запускает сервисы
- ✅ Проверяет здоровье после развертывания
- ✅ Откатывает при неудаче

### 2. 🏥 `health-check.sh` - Мониторинг здоровья
```bash
# Проверка здоровья системы
./health-check.sh
```

**Проверяет:**
- ✅ Статус всех Docker контейнеров
- ✅ Доступность API endpoints
- ✅ Работоспособность frontend
- ✅ Использование системных ресурсов
- ✅ Ошибки в логах

### 3. 🔄 `rollback.sh` - Безопасный откат
```bash
# Откат к предыдущей версии
./rollback.sh
```

**Функции:**
- ✅ Находит предыдущие рабочие образы
- ✅ Создает бэкап текущего состояния
- ✅ Восстанавливает предыдущую версию
- ✅ Проверяет работоспособность после отката

### 4. 🤖 `auto-update.sh` - Автоматические обновления
```bash
# Одноразовая проверка обновлений
./auto-update.sh

# Режим демона (постоянный мониторинг)
./auto-update.sh daemon
```

**Возможности:**
- ✅ Проверяет наличие обновлений на GitHub
- ✅ Автоматически применяет обновления
- ✅ Отправляет уведомления в Telegram
- ✅ Создает бэкапы перед обновлением

### 5. 🧹 `cleanup-server.sh` - Очистка сервера
```bash
# Очистка сервера от мусора
./cleanup-server.sh
```

**Очищает:**
- ✅ Старые Docker образы и контейнеры
- ✅ Временные файлы
- ✅ Старые бэкапы (старше 30 дней)
- ✅ Кэш и неиспользуемые ресурсы

## 🎯 Как использовать систему

### Первоначальная настройка

1. **Клонировать репозиторий:**
```bash
git clone https://github.com/whiterabbit74/stonks.git
cd stonks
```

2. **Настроить переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл
```

3. **Запустить систему:**
```bash
./deploy.sh
```

### Ежедневное использование

1. **Проверка здоровья:**
```bash
./health-check.sh
```

2. **Обновление системы:**
```bash
./deploy.sh
```

3. **При проблемах - откат:**
```bash
./rollback.sh
```

### Автоматизация

1. **Включить автообновления:**
```bash
# В screen или tmux
./auto-update.sh daemon
```

2. **Настроить cron для ежедневной очистки:**
```bash
# Добавить в crontab:
0 2 * * * cd /path/to/project && ./cleanup-server.sh
```

## 🔧 Расширенная настройка

### Telegram уведомления

1. **Создать бота в @BotFather**
2. **Получить токен и chat ID**
3. **Добавить в .env:**
```bash
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

### Мониторинг ресурсов

Система автоматически отслеживает:
- Использование CPU и памяти
- Место на диске
- Количество ошибок в логах

**Пороги предупреждений:**
- Память: >60% (предупреждение), >80% (критично)
- Диск: >75% (предупреждение), >90% (критично)

## 🛠️ Устранение неисправностей

### Проблема: "SyntaxError: Missing initializer in const declaration"

**Причина:** TypeScript синтаксис в JavaScript файле
**Решение:**
```bash
# deploy.sh автоматически проверяет и предотвращает эту ошибку
./deploy.sh
```

### Проблема: Сервисы не запускаются

**Решение:**
```bash
# Проверить логи
docker compose logs

# Проверить здоровье
./health-check.sh

# Откатить при необходимости
./rollback.sh
```

### Проблема: Недостаточно места на диске

**Решение:**
```bash
# Очистить сервер
./cleanup-server.sh

# Проверить использование
df -h
docker system df
```

## 📊 Мониторинг и логи

### Логи системы
```
auto-update.log      # Логи автообновлений
health-check.log     # Результаты проверок здоровья
```

### Бэкапы
```
backup_*/            # Ручные бэкапы
auto_backup_*/       # Автоматические бэкапы
rollback_backup_*/   # Бэкапы перед откатом
```

## 🔐 Безопасность

### Защита от сбоев
- ✅ Всегда создается бэкап перед изменениями
- ✅ Проверка здоровья перед и после обновлений
- ✅ Автоматический откат при неудачах

### Доступ к системе
- 🔒 Все скрипты проверяют наличие необходимых файлов
- 🔒 Откат возможен только к предыдущим рабочим версиям
- 🔒 Логи содержат информацию о всех изменениях

## 📈 Производительность

### Оптимизации
- Автоматическая очистка Docker кэша
- Удаление старых образов (сохраняются последние 3 версии)
- Очистка временных файлов
- Оптимизация использования диска

### Ресурсы
- Минимальное использование CPU во время проверок
- Эффективная очистка памяти
- Оптимизированные Docker образы

## 🎉 Итоговые преимущества

✅ **Надежность:** Автоматические проверки и откаты
✅ **Автоматизация:** Минимум ручных операций
✅ **Мониторинг:** Постоянный контроль здоровья
✅ **Безопасность:** Бэкапы и возможность отката
✅ **Производительность:** Автоматическая оптимизация
✅ **Удобство:** Простые команды для всех операций

---

## 🚀 Быстрый старт

```bash
# 1. Клонировать и настроить
git clone https://github.com/whiterabbit74/stonks.git
cd stonks
cp .env.example .env
# Отредактировать .env

# 2. Первый запуск
./deploy.sh

# 3. Включить мониторинг
./health-check.sh

# 4. Настроить автообновления (опционально)
./auto-update.sh daemon
```

**Система готова к работе! 🎉**