# Визуальный аудит сайта — 2026-09-05

Статус: в работе. Аудит выполняется по текущему браузерному рендеру checkout `main`.

## Область и методика

- Маршруты: `/login`, `/data`, `/enhance`, `/stocks`, `/ema`, `/multi-ticker-options`, `/calendar`, `/split`, `/watches`, `/webull`, `/robinhood`, `/settings`.
- Состояния: пустые, заполненные и доступные после действия пользователя; вкладки, графики, таблицы, формы, модальные окна и действия.
- Ширины: 360, 768, 1024 и 1440 px.
- Для каждой находки фиксируются маршрут/состояние, точный визуальный симптом, серьёзность, доказательство и ожидаемое единое правило стиля.
- Для таблиц сделок отдельное правило: вход и выход должны быть сгруппированы в парные колонки с двумя значениями — «Цена покупки / продажи» и «IBS входа / выхода»; разрозненные колонки считаются несоответствием.

## Матрица обхода

| Маршрут | Состояния/вкладки | 360 | 768 | 1024 | 1440 | Статус |
|---|---|---:|---:|---:|---:|---|
| `/login` | вход, ошибки | ☐ | ☐ | ☐ | ☐ | не проверено |
| `/data` | библиотека, импорт CSV/JSON | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/enhance` | провайдер, форма загрузки | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/stocks` | summary, price, charts, equity, exposure, drawdown, trades, profit, duration, contribution, splits, buy-hold, BAC, BAC4, no-stop-loss, options | ✅ | ✅ | ✅ | ✅ | базовый рендер + 16 вкладок desktop |
| `/ema` | summary, price, deviation, equity, exposure, drawdown | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/multi-ticker-options` | summary, equity, price, charts, drawdown | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/calendar` | календарь, импорт, редактирование дня | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/split` | список, редактирование, удаление | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/watches` | summary, watches, EMA alerts, trades | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/webull` | overview, positions, orders, fills, journal, autotrade, monitor | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/robinhood` | connect/account/dashboard/tools | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |
| `/settings` | general, API, Telegram, interface, autotrade | ✅ | ✅ | ✅ | ✅ | базовый рендер проверен |

## Подтверждённые находки

### V-001 — таблица сделок не группирует вход и выход

- **Маршрут/состояние:** `/stocks?tickers=AAPL` → запущенный бэктест → вкладка «Сделки», desktop 1440 px.
- **Доказательство:** `output/playwright/ui-audit/stocks-trades-1440.png` и свежий DOM-снимок: отдельные заголовки «Цена входа» и «Цена выхода»; IBS находится в конце колонки «Дата входа-выхода» как строка вида `6.7% — 86.3%` и не имеет отдельной пары вход/выход.
- **Серьёзность:** P1 для единства финансового интерфейса; широкая таблица также становится тяжелее для чтения.
- **Ожидаемое правило:** одна колонка «Цена покупки / продажи» и одна колонка «IBS вход / выхода», в каждой — два явно подписанных значения в одинаковом формате на всех таблицах.
- **Статус:** исправлено в коммите `7fd3305`; после исправления проверены desktop и mobile screenshots `stocks-trades-after-1440.png` и `stocks-trades-after-360.png`.

### V-002 — мобильная нижняя навигация перегружена и дублирует смысл иконок

- **Маршрут/состояние:** `/data`, mobile 360 px до исправления.
- **Доказательство:** `output/playwright/ui-audit/data-360.png`: нижнее меню содержало 5 пунктов, включая «Данные» и «Опционы»; «Акции» и «EMA» использовали одинаковую иконку графика.
- **Серьёзность:** P1 для мобильной навигации и различимости разделов.
- **Исправление:** «Данные» и «Опционы» убраны из нижнего меню; для EMA назначена отличающаяся иконка `activity`; сетка нижнего меню изменена на 3 колонки.
- **Проверка после исправления:** `output/playwright/ui-audit/data-mobile-after-nav.png`.
- **Статус:** исправлено, коммит `e78db2b`.

### V-003 — фиксированное нижнее меню перекрывает содержимое на мобильном

- **Маршруты/состояния:** `/calendar` и длинные формы `/ema`, `/multi-ticker-options`, mobile 360 px.
- **Доказательство:** `output/playwright/ui-audit/calendar-360.png`: строка списка праздников продолжается под фиксированным меню; аналогичное перекрытие видно в `ema-360.png` и `options-360.png`.
- **Серьёзность:** P1: часть данных/контролов визуально недоступна без дополнительной прокрутки и неясно, что именно скрыто.
- **Ожидаемое правило:** нижняя навигация не закрывает текущий контент; для длинных экранов нужен гарантированный нижний safe-area/отступ и проверка положения при прокрутке.
- **Статус:** подтверждено, исправление не внесено.

### V-004 — горизонтальные табы обрезаются на узких экранах

- **Маршруты/состояния:** `/settings`, `/watches`, `/webull`, `/robinhood`, mobile 360 px; `/robinhood`, desktop 1440 px.
- **Доказательство:** `settings-360.png` обрывает «Интерфейс», `watches-360.png` обрывает «EMA», `webull-360.png` обрывает «Исполненные», `robinhood-360.png` обрывает «Позиции»; `robinhood-1440.png` обрывает правые вкладки даже на desktop.
- **Серьёзность:** P1: пользователь не видит полный набор доступных разделов.
- **Ожидаемое правило:** таб-лист должен горизонтально прокручиваться без обрезания текста, с видимым affordance (признаком продолжения) и автопрокруткой к активному табу; на desktop тот же компонент не должен терять правые пункты.
- **Статус:** подтверждено, исправление не внесено.

### V-005 — лента категорий `/enhance` не помещается на мобильной ширине

- **Маршрут/состояние:** `/enhance`, mobile 360 px.
- **Доказательство:** `enhance-360.png`: кнопки категорий с эмодзи и подписями обрезаны по краям и читаются как фрагменты (`Все`, `Поп...`, `📈NA` и т. п.).
- **Серьёзность:** P2: фильтр доступен частично и выглядит как сломанная сетка.
- **Ожидаемое правило:** единый горизонтальный scroll-контейнер с целыми кнопками, либо перенос в несколько строк; текст каждой категории должен быть полностью читаем.
- **Статус:** подтверждено, исправление не внесено.

### V-006 — переход 640 px слишком рано ужимает карточки датасетов

- **Маршрут/состояние:** `/data`, сравнение 639/640 px.
- **Доказательство:** `data-639.png` сохраняет 2 колонки с читаемыми названиями; `data-640.png` переключается на 3 колонки, из-за чего названия вроде `American Airlines ...` и `Advanced Micro ...` обрезаются.
- **Серьёзность:** P2: пограничный breakpoint ухудшает читаемость без явной потребности в третьей колонке.
- **Ожидаемое правило:** breakpoint для 3 колонок выбирать по минимальной ширине карточки/контента, а не только по стандартному порогу 640 px; проверить также 600–700 px на реальном тексте.
- **Статус:** подтверждено, исправление не внесено.

### V-007 — аналитический tab-strip и край графика теряют содержимое

- **Маршрут/состояние:** `/stocks?tickers=AAPL` → бэктест → вкладки «Сплиты» и «Опционы», desktop 1440 px; вкладка «Цены».
- **Доказательство:** `stocks-tab-11.png` и `stocks-tab-16.png`: при переходе к дальним вкладкам слева остаётся обрезанный фрагмент соседней подписи; `stocks-tab-02.png`: последняя подпись даты на правом краю графика отображается как `Se…`.
- **Серьёзность:** P2: навигация и временная шкала выглядят обрезанными, крайние значения читаются не полностью.
- **Ожидаемое правило:** активный таб прокручивается в безопасную область с сохранением читаемого соседнего контекста; график получает правый внутренний отступ/правильный формат крайнего label без обрезания.
- **Статус:** подтверждено, исправление не внесено.

### V-008 — действия в мониторинге используют текст вместо единого набора иконок

- **Маршруты/состояния:** `/watches` → «Тикеры», «Сделки» и «EMA»; `/webull` → журнал сделок.
- **Доказательство:** в текущем DOM/исходном рендере мониторинг показывает текстовые кнопки «Закрыть», «Изменить», «Ждать покупку», «Ждать продажу», «Удалить», тогда как `/split` и часть брокерского журнала используют иконки редактирования/удаления.
- **Серьёзность:** P2: одна и та же операция визуально оформлена по-разному, а таблицы становятся шире.
- **Ожидаемое правило:** редактирование, удаление и закрытие — компактные иконки с `aria-label` и подсказкой; текст оставлять только для первичных действий формы.
- **Статус:** подтверждено, исправление не внесено.

## Требования к единому виду таблиц сделок

Проверить на `/stocks`, `/watches`, `/webull`, `/robinhood` и во всех вкладках с историей сделок:

- одна колонка «Цена покупки / продажи» с двумя строками или иным однозначным парным представлением;
- одна колонка «IBS входа / выхода» с тем же парным представлением;
- одинаковый порядок, подписи, выравнивание и формат чисел во всех таблицах;
- действия редактирования и удаления — иконками с понятными `aria-label`/подсказкой, не текстовыми кнопками.

### V-009 — сравнение без маржи встроено в стандартные карточки метрик

- **Маршрут/состояние:** `/stocks?tickers=AAPL` → запущенный бэктест с маржой, summary, desktop 1440 px и mobile 360 px.
- **Требование:** не показывать отдельный блок «Сравнение режимов», а выводить под основным значением мелкую строку вида `vs 69.8% без маржи`.
- **Исправление:** comparison-панель удалена; `metricsGrid` теперь принимает baseline и выводит сравнение внутри карточек итогового баланса, доходности, CAGR, win rate, просадки, числа сделок и профит-фактора.
- **Проверка:** DOM не содержит заголовок отдельного блока (`comparisonBlocks: 0`); визуальные доказательства `stocks-comparison-cards-360.png` и `stocks-comparison-cards-1440.png`.
- **Статус:** исправлено в коммите `45f2623`.

## Ограничения

- До авторизации и без тестовых данных часть заполненных таблиц и графиков может быть недоступна; такие места будут помечены отдельно, а не объявлены проверенными.
- Скриншоты текущего обхода сохраняются в `output/playwright/` и используются как доказательство находок.
- Для `/stocks` после запуска AAPL-бэктеста открыты все 16 вкладок; остальные страницы сняты в базовом состоянии без разрушительных действий и без изменения пользовательских данных.
